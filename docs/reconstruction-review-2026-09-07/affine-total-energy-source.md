# Original-source prescribed-boundary total-energy qualification

September23,2026. Supporting South Fork physics work, not a new playable
scene, engine-motion or performance delivery. The ordered river queue remains open.

## Reuse the completed kinetic evidence

The original kinetic pullback audit6480/session50573 remains terminal; it was
not restarted. Its SHA256 is
`3db5edcef9a413dffaf0c6028658e22188ca0b279ce7a381098c70be55dd443e`.
The new total-energy audit explicitly requires that pinned artifact, verifies
all its protected and implementation hashes, and binds the original predictor,
momentum replay and registered mesh to that chain. It preserves coverage/order
and unsupported records, rather than interpreting stored pass flags as proof.

The shared gravity helper now supplies the existing total-energy class and
this source audit without constructing a pressure metric. On each original
curved-depth source fragment, an independent direct branch integral checks
`g*integral(h*h/2+(z-datum)*h)` and its physical-time rate
`g*integral((h+z-datum)*h_t)`. The moving head terms cancel by continuity;
the dry-front terms vanish. No mean-depth, centroid or resampled-bed substitute
is introduced. Source precision is not increased by exact arithmetic.

The audit re-contracts every certified kinetic primitive gradient against the
original momentum, depth-moment, face-column and exterior-flux rates. It then
adds depth/spatial gravitational work in both physical and canonical momentum
coordinates. Exterior work remains explicit. It does not re-solve either
pressure pole or physical momentum system, nor claim the fan's shallow-water
energy equals the dispersive pressure model's kinetic energy.

## Tests and source-run status

The new16 tests cover lossless serialized evidence reuse with pressure solves
and metric construction forbidden, equality with the complete total-energy
wrapper,1e-400 corruption rejection, changed derivative dimensions, exact datum
mass/rate work, independent fresh-time branch quadrature and coverage corruption.
The first focused run passes39 tests in25.31s. The expanded suite passes108
tests in61.00s, including the existing reflecting and prescribed-boundary
energy/derivative controls. No tolerance was relaxed. The
[test receipt](affine-total-energy/source-tests.json) binds the exact scripts,
new tests and both XML outputs (expanded XML elapsed60.98s).

Source audit9552/session56686 was started using the completed kinetic evidence;
case0 passed. A complete source report is not claimed until that process exits
successfully and the final report/hash/coverage checks are recorded. Do not
duplicate this audit or use the obsolete PID6480 profiling wrapper.

## Remaining gates

This is a prerequisite for a compatible conservative force/transport/time step,
not that step itself. Front interaction/activation, slope junctions and natural
or radiating pressure boundaries remain open. No native solver is enabled.
No captured data, map, collision, installed4950s fields or material changed, and
no unrelated game rebuild/capture was repeated for this Python-only increment.
The last measured normal26.484402FPS/p9547.2133ms still fails the30FPS target.
Actual in-game realism, motion, collision, shore/surface continuity and measured
cost must pass before South Fork can be accepted or Colorado work can start.
