# Unreal Photoreal Open-World Stack

The first Unreal slice targets UE 5.8 and enables the modern photoreal river-corridor stack in source-controlled project configuration.
Landscape, rocks, banks, and foliage are treated as first-class immersion targets, with each river corridor pushed as close to photorealistic reference quality as target hardware, VR comfort, and gameplay readability allow.

## Production Baseline

- Nanite for rocks, canyon walls, terrain detail meshes, raft parts, scanned props, and debris.
- Lumen/Lumen Lite evaluation for canyon bounce lighting, wet-rock reflections, forest shade, weather, and portable performance modes.
- Virtual Shadow Maps for dense foliage and dynamic raft/crew shadows.
- World Partition for real-world river corridors.
- PCG for foliage, rocks, gravel bars, driftwood, debris, foam-line accents, and hazards.
- Niagara for white water spray, mist, splash, foam, rain, paddle effects, and rescue cues.
- Substrate/material-layering workflows for wet rock, rubber, mud, foam, helmets, PFDs, and aerated water.
- OpenXR for VR.

## Evaluation Only

- Mesh Terrain, Procedural Vegetation Editor, Fast Geometry Streaming, and Nanite foliage remain feature-evaluation items until they pass editor, performance, memory, and VR comfort checks.
- Unreal Water System is enabled for visual water and authoring, but the custom shallow-water solver remains authoritative physics.

The canonical feature manifest is `unreal/Content/RaftSim/Rendering/photoreal_stack.manifest.json`.

The first vertical-slice environment recipe is `unreal/Content/RaftSim/Rendering/vertical_slice_environment_corridor.json`. It binds the South Fork corridor package, photoreal stack, and telemetry presentation cues into map-authoring layers for landscape/bed, banks, rocks, foliage, debris/access context, lighting/weather, audio occlusion/reverb geometry, and water-readability support.

The three-river environment source and capture plan is `unreal/Content/RaftSim/Rendering/photoreal_river_environment_sources.json`. It records the source stack for South Fork American, Colorado River, and Pacuare: maps, terrain, hydrography, flow evidence, aerial/satellite imagery, link-only social/reference media policy, procedural generation layers, target Unreal map packages, and required guide-seat downstream captures.

The concrete source attachment checklist for tools is `physics/data/real_world/production_geospatial_attachment_ledger.json`. It records existence-checked terrain, hydrography, aerial imagery, mask, flow, access/protected-area, and guide/media annotation artifacts for all three runnable rivers, while keeping every class preview-only until CRS, terms, guide review, rights, and lifelike capture gates pass.

The approved first-party procedural-equivalent environment asset plan is `unreal/Content/RaftSim/Rendering/first_party_procedural_environment_assets.json`. It covers canyon walls, riverbeds, wet boulders, shore vegetation, tropical canopy, water depth/current cues, foam/spray/mist, raft foreground, and river-specific lighting as the traceable replacement path when licensed/photogrammetry assets are not yet cleared. The editor preview and capture commands now require and record this plan, but the current renders remain preview-only until these recipes are implemented as production-quality Unreal assets/materials and verified by capture/performance evidence.

The first generated preview maps are now available under `/Game/RaftSim/Maps/EnvironmentPreviews/`, and rendered guide-seat captures are written to `docs/environment-captures/photoreal_river_previews/` by the `RaftSim.CreatePhotorealEnvironmentPreviewMaps` and `RaftSim.CapturePhotorealEnvironmentPreviews` editor automation. South Fork, Colorado, and Pacuare now have source-draped preview overlays, source-drape terrain vertex-color blending, generated valley terrain, curved river ribbons, foam/hydraulic cue strips, first-party translucent water glints/foam flecks/haze/spray-mist layers, boulder bars, source-mask-biased near-field riverbed pebble/debris dressing, source-masked shoreline lip/overhang edge breakup, first-party source-aware terrain surface granularity, first-party photographic color-grade/palette compression with bounded film-grain dither, clustered foliage proxies, and per-river light/fog variants; the automation also scopes UE 5.8 startup-world GC leak checks to nonfatal during generated-map/capture batches so evidence capture can complete while diagnostics remain logged. The latest pass disables the former visible tapered water-chroma overlay and separate water fleck cards, moves bounded chroma/noise/relief and fleck cues into integrated water meshes, adds a source-aware faceted-water blocker, smooths/demotes near-field cellular water relief in the guide-seat capture layer, adds smooth current-thread color diversity and Pacuare rainforest reflection bands, adds source-mask-biased shoreline lip/overhang meshes to interrupt ruler-straight bank/water edges, adds denser terrain mesh granularity to interrupt broad smooth slopes, bounds capture saturation/contrast/exposure settings, adds a restrained engine-side film-grain/dither hook, applies capture post-processing directly to the scene-capture component, and disables the guide-seat raft/oar foreground proxy hooks that were causing a blade-shaped near-field obstruction. The current automated review records `low_color_texture_entropy` blockers across the six base guide-seat/river-eye captures and the 20 flow-variant captures, so the screenshots remain preview-only until material, water, terrain, and source-conditioned detail are rich enough for zero-blocker human lifelike review. `docs/environment-captures/photoreal_river_previews/photoreal_human_lifelike_review_handoff.json` and `docs/environment-captures/photoreal_river_previews/photoreal_human_lifelike_review_packet.md` remain the seven-domain review scaffolds with embedded images, source/flow context, metrics, review prompts, and signoff checkboxes, but no capture is approved as lifelike or production-ready. `docs/environment-captures/photoreal_river_previews/photoreal_environment_performance_review.json` records the desktop/VR performance evidence scaffold and static map/capture/material inventory, but leaves measured profile slots open and unapproved until real Unreal profiling evidence is attached. `docs/environment-captures/photoreal_river_previews/photoreal_flow_variant_capture_plan.json` expands the 10 South Fork, Colorado, and Pacuare flow bands into 20 required band-named guide-seat and river-eye captures, and `docs/environment-captures/photoreal_river_previews/flow_variant_capture_manifest.json` records the first complete flow-variant Unreal pass with 10 generated variant maps and 20 PNGs. Final approval still requires actual guide/art/geospatial/hazard/rights/solver-forcing/performance evidence plus replacing smooth procedural terrain, placeholder rocks/foliage, review-only material instances, and missing production foam/mist/lighting with full reviewed DEM/aerial imports plus rights-cleared or first-party production terrain, water, rock, foliage, foam/mist, lighting, guide/geospatial/hazard review, and performance evidence.

The first source-conditioned material replacement candidates now live under `unreal/Content/RaftSim/Rendering/SourceConditionedMaterialMaps/`. The manifest records 2048px macro albedo, material-zone, packed AO/roughness/height, and DEM/mask/source-luma-derived normal-detail maps from the active review-gated aerial drapes, DEM relief, water masks, and vegetation masks for all three rivers. The current `source_luma_wet_vegetation_microdetail_v2` pass adds bounded source-drape luma variation to banks, rocks, vegetation, wet edges, roughness, height, and normal-detail candidates, while keeping visible-water normal strength low and recording that the maps must not invent or hide hydraulic geometry. Editor automation now imports the 12 maps as review-only `Texture2D` assets, binds them to the atlas candidate material instances, exposes low-gain zone/macro-albedo/surface-response/normal-detail controls, regenerates the guide-seat and river-eye captures, and refreshes the automated/human-review handoff with the new hashes. The next rendering step is human art/guide/geospatial review and true production material promotion before any lifelike claim.

The latest Unreal automation refresh re-imported the review textures, rebuilt the base and flow-variant preview maps, recaptured the guide-seat/river-eye PNGs, and regenerated the automated review, human-review, and performance scaffolds. The refreshed captures no longer show the dark guide-seat foreground obstruction, but they still record `low_color_texture_entropy` blockers and read as simplified proxy terrain, water, and rocks rather than photoreal production art.

The recipe requires guide-seat review captures, desktop/VR/debug quality budgets, source and rights manifests, and replay alignment between rendered water features and solver/runtime telemetry before the environment can count as milestone-complete.
