# Zambezi And Futaleufu Photoreal Goal

> Active priority decision, July 14, 2026: Zambezi Batoka Gorge and Futaleufu are the immediate photoreal environment goal. Zambezi returns from backlog as an additional active environment target, while runnable and lifelike promotion remains blocked until authoritative full-reach centerline, high-resolution terrain, route, guide, and review evidence are attached. Chilko remains in the five-river runnable portfolio through `docs/chilko-futaleufu-photoreal-goal.md`.

## Objective

Make these two priority river environments photorealistic in Unreal:

- Zambezi River, Batoka Gorge, from Boiling Pot to Mukuni Beach: a photoreal big-water basalt gorge run below Victoria Falls.
- Futaleufu River, Chile, from Rio Azul Swinging Bridge to The Pasarela: a photoreal Patagonian turquoise big-water run with boulder gardens and forested Andean valley walls.

Futaleufu is required by the active five-river runnable goal. Zambezi is an additional active environment target whose runnable promotion remains source-blocked. For both rivers, completion means lifelike in-engine guide-seat and river-eye captures at reviewed seasonal flows, backed by source-traceable terrain, hydrography, materials, foliage, water, atmosphere, named hazards, and accepted art, guide, geospatial, rights, hazard-readability, desktop, and VR review. Source-data packages, generated maps, or technically valid screenshots alone do not complete the goal.

## Current Evidence And Blockers

Both rivers now have reproducible technical corridor packages with named-route geometry, source-recorded Copernicus DEM GLO-30 terrain, route-window Sentinel-2 imagery, seasonal-flow context, derived Unreal texture inputs, and physical-scale Unreal Landscape candidate maps. The international centerline contract now follows source image rows southward from the northwest heightfield corner, matching the established South Fork and Colorado import convention.

The July 14 custodian audit found no downloadable Batoka survey payload in the official ZAMCOM/ZAMWIS public catalog: it exposes 65 thematic layers, but its listed elevation product is a generalized 1 km layer and no Batoka LiDAR, high-resolution terrain, breakline, or surveyed-centerline layer appears. The July 15 coverage audit then separated the known products: the public 0.3 m DTM supports a downstream Batoka dam-to-Lake Kariba dam-break model, the approximately 5 cm/13 square kilometre eBee DSM is dam-site work, and the World Bank's additional LiDAR belongs to the separate Kariba Dam Rehabilitation Project. None proves playable-reach coverage. Feasibility Vol.3 remains coverage-unknown, so the request now asks for its report and coverage index before any payload. ZAMCOM/ZAMWIS and the World Bank Access to Information route remain part of the custodian chain, while outreach is unsent pending project-owner sender and cost authority. This closes misleading acquisition paths; it does not close the terrain or centerline gate.

The first imagery-observed route comparison now reads the selected June 10, 2026 Sentinel-2 green and NIR COGs directly at 10 m in WGS 84 / UTM zone 35S. A bounded dynamic route snaps the OSM planning line no farther than 200 m, improving thresholded water support from 89.34 to 95.67 percent with a 9.4 m median and 31.9 m p95 correction. A route-topology guard rejects 1,377 lateral candidates that are materially closer to a nonlocal branch; the overlay preserves the resulting unguarded 196.25 m island shortcut in magenta, the topology-guarded candidate in cyan, and the OSM route in yellow. The GeoJSON separately stations rejected raw and retained guarded corrections above 100 m, and the NDWI, overlay, and measurements are hash-locked. This is a surface-water observation for manual comparison and future survey QA, not surveyed centerline, banks, bathymetry, rapid geometry, or production authority; the existing corridor remains unchanged.

Fresh offscreen Unreal captures prove that the Futaleufu river ribbon, cameras, and DEM valley are registered after that transform correction. They also prove that neither environment is lifelike yet. Current blockers include:

- Zambezi's current OSM route is a low-precision international-boundary trace rather than a surveyed centerline; its 30 m DSM cannot resolve the narrow gorge floor and the current bounded visual conditioning reaches a review-gated 218.8192 m maximum cut.
- Coarse channel geometry still needs higher-resolution terrain, surveyed sections, lidar/photogrammetry, or guide-reviewed first-party field reconstruction before close river-level approval.
- Generic Procedural Vegetation Editor species, sparse or ecologically inaccurate silhouettes, and no approved river-specific species/age/density libraries.
- Shared fallback near-field detail textures instead of unique reviewed Zambezi basalt and Futaleufu granite/temperate-rainforest material sets.
- Review water shading without production reflection, refraction, foam, spray, mist, wet-bank transitions, or live solver-derived surface fields.
- No approved low/normal/high seasonal-flow capture set, guide/art/geospatial/rights review, or measured desktop/VR performance evidence.

The current Landscape candidate captures are diagnostic evidence only and must not be presented as photorealistic screenshots.

The July 11 conditioned-profile pass removed the previously captured cross-stream water walls. Zambezi now uses all 30,137 one-metre ribbon samples from its manifest-recorded non-rising 95.0141 m visual profile; Futaleufu uses all 16,060 samples from its non-rising 134.5572 m profile. Both profiles are render-only, have zero recorded terrain fill, and leave custom C++/GeoClaw physics authority unchanged.

The July 11 terrain-projection pass corrected the north-up source-drape UV on the physical render mesh, stopped coarse 25 m vertex color from overriding the imported source texture for Zambezi/Futaleufu, moved the 4K detail maps to 9-18 m real-world footprints to suppress river-eye moire, and added bounded render-only steep-slope relief on a 12.5 m mesh. The final UE 5.8 pass records 1,631,500 vertices/3,251,726 triangles for Zambezi and 543,510/1,080,040 for Futaleufu, plus 180 instances per river from the six-variant rights-reviewed CC0 Nanite rock set. All five generated maps pass Unreal map check with zero errors and warnings. Landscape collision/height queries and the manifest-conditioned river profile remain authoritative; this denser mesh is visual only.

A river-specific trial of the already isolated Poly Haven `Tree Small 02`, fir, and pine assets was rendered and rejected in the same pass. Zambezi became overexposed with repeated sparse crowns, while Futaleufu's conifers collapsed into bare vertical silhouettes at gameplay distance. Those substitutions were removed before the final captures. The existing PVE trees remain temporary blockers, and neither the rejected tree assets nor the retained rock comparison is ecology, geology, lifelike, performance, or gameplay promoted.

A second Zambezi canopy experiment now proves the full rights-reviewed external-tree intake path with Poly Haven `Jacaranda Tree`: eleven source files are hash-locked, the isolated UE 5.8 import has a grounded 19.47 m Nanite mesh with explicit branch/trunk/leaf materials, and the measured FBX contains 3,133,049 source triangles despite the publisher page's `312K` label. The visual comparison is rejected and preserved in `polyhaven_jacaranda_tree_visual_comparison_review.json`: leaf cards render as an obstructive black/yellow mass, the terrain falls back to a bright white appearance, distant crowns repeat, and the structure analog has no Batoka species approval. The default map generator and regenerated candidate manifest contain no Jacaranda reference; the restored rock/PVE baseline again passes all five map checks with zero errors and warnings. The next Zambezi foliage acquisition must provide multiple rights-cleared, distance-stable southern African gorge woodland species and material variants, not another single-tree substitution.

The first multi-strata Futaleufu foliage intake now validates ten isolated Poly Haven meshes: three 5.89-8.81 m medium firs, three 0.74-1.30 m small firs, and four 0.21-0.43 m fern clumps, backed by 21 hash-locked sources, 18 textures, five explicit materials, grounded pivots, and Nanite. The experiment also fixed a shared import defect: idempotent sampler repair now classifies texture names containing `Alpha` as masks, preventing Metal from substituting the default material. A clean-start comparison then proved the set itself is not a corridor-scale improvement: medium fir needles collapse into pale branch silhouettes, saplings/ferns disappear at river-camera distance, and replacing 9,600 PVE placements reduces continuous green slope breakup. `polyhaven_futaleufu_temperate_forest_set_visual_comparison_review.json` rejects promotion, the default generator excludes the set, and the restored baseline passes 12 zero-error/zero-warning map checks. The next canopy acquisition must prioritize multiple adult, dense, distance-stable Patagonian broadleaf or reviewed Nothofagus-family variants; the validated sapling and fern assets are only candidates for closer near-bank strata.

The follow-up adult-crown intake validates three isolated Poly Haven Island Tree meshes from 33 hash-locked CC0 files. The importer now scales their crowns to the publisher's 8.5 m and 12.5 m widths, producing grounded 6.87-13.05 m Nanite meshes with 30 textures and nine explicit trunk, masked-leaf, and branch materials. That technical success still fails the environment-art gate: replacing one in five distant Futaleufu foliage placements produces conspicuous black branch-card marks in both downstream viewpoints and does not establish native Patagonian or Nothofagus identity. `polyhaven_futaleufu_island_tree_set_visual_comparison_review.json` preserves the comparison and rejects promotion; the default generator excludes the set. The next intake must couple native or ecologically reviewed species diversity with authored distance-stable crown behavior, Metal/Nanite leaf validation, forest-band ecology, and measured performance.

The next intake now uses UE 5.8's installed Procedural Vegetation Editor as a reproducible structural diagnostic rather than another immediate map substitution. `RaftSim.InspectProceduralVegetationSample` records the 26-node source graph and wiring, while `RaftSim.EvaluateProceduralBeechCandidate` loads four hash-locked European Beech growth datasets through the real PVE loader, replaces the sample grower at the mesher input, and executes PCG headlessly. Beech 01-04 produce 55,138-78,839 trunk vertices, 108,632-154,109 triangles, five complete twig meshes, and 201-815 foliage instances. The rerunnable headless path writes ignored local-only trunk assets and foliage templates, then checks LOD0 Nanite fallback geometry, positive bounds, non-null materials, Nanite enablement, and `PreserveArea`; measured heights span 8.33-25.34 m. It must run with offscreen Metal because UE 5.8 PVE executes `FPVLightDetectionCS`, which has no `NullRHI` shader permutation. Only the portable structural reports are retained in the repository. These remain installed-engine diagnostics: no Epic sample content or derived mesh is copied into the open-source tree, Fagus sylvatica is not native Nothofagus, and isolated visual/material, river-view, ecology, rights, guide, and performance gates remain open.

The isolated PVE visual gate is now complete and rejects all four variants before a corridor substitution. Twelve local-only captures cover two turntable azimuths and a 60 m river-distance proxy, with zero map-check errors/warnings and hashes retained in `ue_pve_futaleufu_european_beech_visual_review.json`. Beech 01/02 are tall but sparse and top-heavy, Beech 03 nearly disappears at river distance, and Beech 04 is denser only at close range while remaining a short dark shrub-like mass. The installed twig materials are too dark under fixed Futaleufu photometry, none of the crowns provides layered rainforest mass, and the non-native Fagus identity remains disqualifying. No bounded corridor substitution was performed; the next intake must be rights-clear native Patagonian or ecology-reviewed Nothofagus-family foliage with broad distance-stable crowns.

The native-canopy acquisition audit now replaces vague "Patagonian foliage" language with `futaleufu_native_canopy_authoring_spec.json`. CONAF identifies low-valley evergreen forest dominated by coigue with tepa, manio, and radal; higher lenga forest with nirre and coigue de Magallanes; and north/east cordillera-cypress groves. Chilebosque's coigue morphology adds the measurable adult target: up to 40 m, a relatively straight cylindrical trunk, dark gray vertically fissured bark, dense foliage, strong open horizontal branches, and 2-3.5 cm evergreen leaves. Fab searches found no exact Nothofagus/coihue/lenga asset, so the next implementation is a project-owned `Nothofagus dombeyi` prototype, followed by the same isolated Metal, distance, ecology, rights, and performance gates before corridor use.

The first project-owned coigue pass now proves the complete source-to-Unreal prototype path: two generated source studies become six reproducible 2048-pixel texture candidates, explicit bark and Metal two-sided-foliage materials, separate trunk and foliage static meshes, Nanite `PreserveArea`, and a rerunnable five-view offscreen Metal review map. The result is deliberately not promoted. Its straight bole, horizontal branching, and crown survive the 60 m and 150 m proxies, but the closeup and turntables expose 225-283 cm crossed cards as flat hanging sheets, the bark texture loses readability in shadow, and one regular adult form cannot establish native forest ecology. `futaleufu_native_canopy_coigue_visual_review.json` preserves all five hashes and rejects corridor substitution. The next iteration must use denser 0.55-1.10 m branch-aligned sprays, improve neutral bark readability, add wind controls and adult/intermediate crown variants, then repeat the same isolated gate.

The v2 coigue pass replaces the rejected branch-spray source with a project-owned 4x4 atlas containing twelve isolated leaf studies and four tiny twig studies, then separates physically scaled near leaves from a denser far-crown representation. The near mesh retains 193,536 masked cards and 387,072 triangles; the far mesh retains 56,448 cards and 112,896 triangles; the explicit handoff overlaps from 28 m to 36 m; and `WindIntensity`, `WindWeight`, and `WindSpeed` are exposed. A controlled UE 5.8 Metal comparison also establishes a necessary Nanite exception: trunk and branches remain Nanite `PreserveArea`, while masked foliage retains full non-Nanite source geometry because `PreserveArea` expands the microcards into large opaque triangles and `None` simplifies them away. The calibrated 60 m and 150 m views retain a dense dark crown without the v1 sheet artifacts, but `futaleufu_native_canopy_coigue_v2_visual_review.json` still rejects corridor substitution. Main branches remain too regularly tiered and conifer-like, the closeup shows unattached volumetric cards instead of secondary branchlets and petioles, the far representation repeats, and only one adult form exists. V3 must author irregular branchlet topology plus open-grown, forest-grown, and intermediate crown variants before another corridor comparison.

The v3 coigue pass replaces the independent near-card volume with 260 foliage anchors, 1,040 modeled secondary branchlets, two tertiary branches per branchlet, and 14,560 single leaves attached through modeled petioles. Its first Unreal run exposed a second Nanite boundary: merging centimeter-scale woody detail into the trunk's `PreserveArea` mesh expanded branchlets into pale wedges. The selected split keeps only the structural trunk and limbs in Nanite, while a separate non-Nanite bark mesh retains 126,880 source triangles and renders 201,760 split vertices without simplification; it shares the near foliage's 36 m cull range. Five corrected captures pass map check with zero errors and warnings, but `futaleufu_native_canopy_coigue_v3_visual_review.json` still rejects promotion. The whole crown remains sparse and tiered; closeup branchlets are overlong, intersecting, and too lightly leafed; and open-grown, forest-grown, and intermediate forms are still absent. V4 must use short dense source-matched shoots and clustered crown mass before another corridor comparison.

The v4 coigue pass now produces the first continuous distance-stable crown: 388 branch-attached near anchors carry 2,328 short shoots and 32,592 attached leaves, while 240 far-only interior anchors distribute 90,432 masked twig cards through the crown instead of only tracing branch shelves. Material debugging also closes two false-fidelity paths. `TwoSidedSign` now corrects tangent normals on leaf backs, and alpha is connected as scalar texture output 4 without a redundant component mask; the final Metal run has no material fallback and removes the silver card field. A second controlled geometry comparison disables Nanite on the inexpensive 1,624-vertex trunk because `PreserveArea` facets its tapered tips in closeup. `futaleufu_native_canopy_coigue_v4_visual_review.json` still rejects promotion: the closeup exposes crowded/intersecting branch origins and abrupt joins, horizontal shelves remain legible, bark and interior foliage are too dark, and multiple crown forms plus temporal/performance/ecology evidence remain absent. V5 must replace radial bundles with clean parent-child ramification and author open-grown, forest-grown, and intermediate forms before corridor use.

The v5 coigue pass closes the floating-bundle topology defect. Near geometry now starts at 234 limb-attached parent shoots; six branchlets originate at distinct positions along each parent, tertiary branches originate along branchlets, and 19,656 leaves retain modeled petioles. Moving the 24 apical bundles to far-only status and reducing each main branch from fourteen to nine near anchors lowers the branchlet mesh from 238,374 to 149,514 source triangles. Replacing removed distance mass with 370 interior far anchors preserves the same 90,432-card far crown and continuous 60/150 m silhouette. The tangential closeup is substantially clearer, but `futaleufu_native_canopy_coigue_v5_visual_review.json` still rejects promotion: it exposes a hard cylindrical primary-limb cutoff and abrupt parent junction, bark and interior foliage remain too dark, and only one open-grown adult exists. V6 must blend woody junctions and parameterize open-grown, forest-grown, and intermediate crown forms before temporal or corridor gates.

The v6 coigue pass closes the hard woody-junction defect. Every primary branch now continues through 7.0-to-3.2-to-0.8 cm tip tapers; every terminal secondary twig continues through a 3.2-to-1.4-to-0.72 cm collar into a 0.74 cm parent shoot; and branchlet plus tertiary radii decrease monotonically after that. The extra transitions add only 936 trunk vertices and 1,716 branchlet triangles, and the tangential closeup no longer shows the v5 cylindrical cutoff. `futaleufu_native_canopy_coigue_v6_visual_review.json` accepts this junction hierarchy but still rejects promotion because bark/interior foliage remain too dark and only one regular open-grown adult exists. V7 must parameterize open-grown, forest-grown, and intermediate forms, capture all five views for each, and rebalance physically lit material response before temporal or corridor gates.

The v7 coigue pass replaces the single hard-coded crown with three independently generated forms. Open-grown adult, intermediate, and forest-grown adult now use separate deterministic seeds plus distinct trunk height/radius, live-crown base/span, crown width, branch uplift, asymmetry, main-branch count, and interior-anchor density; the Unreal command saves twelve independent static meshes rather than scaled copies. A three-form review map captures the same 35/145-degree, closeup, 60 m, and 150 m views for each form. Bounded exposed `BarkBaseColorScale`, `BarkAOInfluence`, `LeafBaseColorScale`, `LeafAOInfluence`, and `LeafTransmissionTint` controls improve leaf and interior-crown readability under fixed exposure without emissive fill. The final UE 5.8 Metal run records zero map-check errors/warnings and no material compile failures, while all 39 environment tests pass. `futaleufu_native_canopy_coigue_v7_visual_review.json` nevertheless rejects corridor substitution: three prototypes do not satisfy the five-adult/three-intermediate ecology contract, repeated shelf cadence and sparse closeup detail still read procedurally, shadow-side bark remains too dark, and temporal wind, measured performance, mixed-species ecology, and corridor comparison remain open.

The v8 coigue pass closes the dominant-species family-count gate without using transformed copies. Five adult forms now cover open growth, forest competition, deterministic storm damage, competition lean, and crown gaps; three intermediate forms cover baseline, suppressed, and released growth. New trunk-lean, suppressed-sector, vertical-gap, and damage parameters alter source geometry, while reports derive actual near-anchor counts from generated leaf topology. The storm form has six deterministic broken branches, 177 near anchors, and 14,868 attached leaf cards versus its undamaged 225-anchor budget. The first forty-view run exposed white leaf fallback quads caused by material compilation before texture mips existed. The selected fix explicitly finishes each native texture build before package save, uses resident Sharpen5 leaf mips with 0.50 alpha coverage, and forces residency before material compilation. A final pass spaces forms 5 km apart, captures all five views per form, and records no fallback quads, texture no-miplevel warnings, material errors, or map-check errors/warnings; all forty hashes and all 39 environment tests verify. `futaleufu_native_canopy_coigue_v8_visual_review.json` still rejects corridor substitution because foreground twigs/leaves remain sparse and intersecting, shared primary-branch cadence remains visible, shadow-side bark is too dark, and temporal wind, measured performance, sapling/deadwood and mixed-species ecology, plus source-matched corridor evidence remain open.

The v9 coigue pass keeps the eight-form family but replaces sparse foreground ramification with shared, report-locked topology constants: eight golden-angle branchlets per parent shoot, three tertiaries per branchlet, and twenty-two attached leaves per branchlet. Branchlets shorten from 30-49 cm to 24-40 cm as density rises. Open-grown near foliage increases from 19,656 to 41,184 cards and its non-Nanite branchlet mesh from 243,834 to 468,872 render vertices; the other forms scale from their actual retained anchor counts. A bounded opposing 0.38 fill and reduced 0.75 front fill reveal bark texture in both turntable azimuths while preserving fixed exposure, the same material response, shadow casting on the tree, and zero emissive lift. The forty-view Metal run remains distance-stable and free of material, texture-mip, and map-check errors/warnings, and all forty hashes plus 39 environment tests verify. `futaleufu_native_canopy_coigue_v9_visual_review.json` accepts the density and isolated bark-readability improvements but rejects corridor substitution: closeups still expose branchlet/tertiary intersections, all forms retain some shared primary cadence, the near-detail cost almost doubled without desktop/VR measurements, temporal wind is unvalidated, and mixed native ecology plus corridor evidence remain absent.

The v10 coigue pass adds deterministic collision-aware routing without weakening the v9 topology or physical thresholds. Each near branchlet and tertiary is checked against previously authored nearby centerlines; the original authored placement is tried first, followed by bounded angular alternatives and small parent-axis attachment shifts. The final family reroutes 16 branchlets and 902 tertiaries, all eight forms pass the 1.2 cm branchlet and 0.6 cm tertiary near-clearance contracts, and no unresolved conflicts remain. UE 5.8 compiles cleanly, records forty zero-error/zero-warning map checks, validates 41 generated assets, and reproduces the forty-view capture set. `futaleufu_native_canopy_coigue_v10_visual_review.json` nevertheless keeps corridor promotion closed: the centerline audit does not cover parent/trunk junctions, leaf overlap, or screen-space silhouette quality; closeups still read as visually tangled procedural assembly; primary cadence, temporal wind, desktop/VR cost, and mixed native ecology remain open.

The v11 instrumentation pass measures the cost that v9 density introduced without changing geometry or visuals. Across 32 generated meshes, the eight isolated forms occupy 367,918,128 exclusive resource bytes (350.87 MiB), 6,961,948 LOD0 render vertices, and 3,859,140 LOD0 render triangles. Forty valid offscreen capture wall-time samples have a 241.35 ms median, but this is an authoring-path measurement rather than gameplay frame time. Unreal's unattended game/render/GPU frame-cycle counters are rejected automatically because the game-thread values are zero and render/GPU values are invariant. `futaleufu_native_canopy_coigue_v11_performance_review.json` therefore keeps promotion closed pending reduced LODs, corridor-density HISM/PCG placement, masked-overdraw/draw/streaming/culling evidence, and packaged desktop plus on-device VR profiling. The V10 visual, temporal-wind, and ecology blockers also remain.

## Completion Criteria

- The route, DEM, channel, imagery, and Unreal local transforms remain reproducible, source-recorded, and covered by regression tests.
- The visual channel and water profile are hydrologically conditioned with bounded cut/fill and slope parameters recorded in the production manifest; this visual conditioning cannot alter or conceal custom C++ solver validation.
- River width, bank geometry, water level, surface response, foam, spray, mist, wetness, and atmosphere vary by reviewed seasonal flow band.
- Zambezi uses approved basalt gorge, riparian scrub, and southern African gorge-woodland assets; Futaleufu uses approved Patagonian granite, gravel-bar, temperate-rainforest, and Andean assets with enough variation to avoid repetition.
- Guide-seat and river-eye cameras show coherent downstream river corridors with no terrain clipping, water walls, self-intersections, exposed seams, placeholder cards, or obvious proxy geometry.
- Fresh offscreen Unreal captures pass automated artifact checks plus human art, guide, geospatial, rights, hazard-readability, and desktop/VR performance review.

## Shared Promotion Gates

Neither river can be called lifelike, production-playable, or screenshot-ready until these gates are attached and reviewed:

- Source manifests for terrain, hydrography, access/publication constraints, protected areas, and route stationing.
- Seasonal or release/rain/snowmelt flow bands with source, units, time zone, station relation, and guide review.
- Aerial/satellite imagery or first-party field capture plans with provider, acquisition date, CRS, terms, and processing provenance.
- Link-only public/social/reference media queues, with no downloaded or promoted third-party media until item-level rights are explicit.
- First-party or rights-cleared procedural material, foliage, rock, mist, foam, water, and atmosphere recipes.
- Unreal preview maps, guide-seat captures, river-eye captures, automated capture-quality review, human lifelike review, and desktop/VR performance evidence.
- Validation that visual/feature forcing is bounded, manifest-recorded, flow-dependent, GeoClaw/C++ compared where relevant, and not hiding conservation or raft-coupling failures.

## Zambezi River, Batoka Gorge

Target the Batoka Gorge character first: steep dark basalt walls, constricted canyon scale, warm spray and mist, strong wave trains, laterals, boils, holes, surging eddy lines, and flow-dependent hole stickiness or washout. Gameplay tuning must support commercial big-water rafting lines while preserving visibility for swimmers, pins, flips, rescues, and high-side timing.

Initial data plan:

- Maintain the current Copernicus DEM/Sentinel/OpenStreetMap technical corridor while seeking higher-resolution terrain, bathymetry, and guide-reviewed station data.
- Prioritize the official BGHES survey acquisition path recorded in `physics/data/real_world/zambezi_batoka_gorge/terrain/source/batoka_high_resolution_terrain_acquisition_leads.json`, but keep the products distinct. The Dam Safety Plan's 0.3 m terrain database is explicitly tied to an approximately 120 km model downstream from the proposed dam to Lake Kariba; the approximately 5 cm eBee DSM is a separate 13 square kilometre dam-site survey; and World Bank completion reporting assigns another LiDAR activity to the Kariba Dam Rehabilitation Project. None proves coverage of Boiling Pot to Mukuni Beach. Feasibility Vol.3, `230 GEN R SP 001`, remains the only engineering-survey lead with unknown and potentially relevant coverage. `docs/batoka-high-resolution-terrain-acquisition-request.md` now asks for its report and coverage index first, then only payload that intersects the exact reach bounds. No underlying data or reuse rights are attached. The request is ready but has not been sent because sender identity and authority to accept quoted license or data costs require project-owner confirmation. Never digitize report figures as production geometry.
- Validate the recorded Zambezi River Authority Nana's Farm flow context and proposed low, normal big-water, high, and extreme review bands with local guides.
- Build link-only reference queues for official tourism/park/outfitter pages, guide footage leads, and public media without downloading or training on third-party assets.
- Add protected-area, access, cross-border Zambia/Zimbabwe publication, and sensitive-location review before public screenshots.
- Use procedural basalt strata, wet rock, plunge mist, canyon haze, warm river color, foam, and spray as first-party placeholders until source-conditioned or approved assets replace them.

## Futaleufu River, Chile

Target the Futaleufu character first: clear turquoise water, high-energy wave trains, granite or metamorphic boulder gardens, steep forested valley walls, gravel bars, Andean background, and strong seasonal variation from snowmelt and rain. Gameplay tuning must support powerful hydraulics, boulder pushes, pins/releases, flips, swimmer drift, and rescue readability.

Initial data plan:

- Maintain the current Copernicus DEM/Sentinel/OpenStreetMap technical corridor while seeking higher-resolution Chilean terrain, bathymetry, access, and protected-area data.
- Validate the recorded DGA station context and proposed low, normal runnable, high, and unsafe/flood-style looks, including station-to-reach translation, with guide/outfitter review.
- Build link-only reference queues for official regional sources, guide/outfitter media leads, field-capture requests, and public/social pages without importing third-party media.
- Use procedural turquoise water depth, wet granite/boulder surfaces, temperate rainforest/valley foliage, snowmelt atmosphere, foam, and mist as first-party placeholders until approved assets replace them.
- Preserve hazard readability for big-water features, shorelines, swimmers, rescue targets, and raft contact outcomes while art quality improves.
# Named Rapid Integration Update

The five-river rapid catalog and review-run plan are documented in `docs/named-rapid-realism-validation-plan.md`. Zambezi now has editor markers for Rapids 1 through 25, including mandatory commercial-portage-only treatment for Commercial Suicide, and Futaleufu has the Rio Azul-to-Pasarela sequence through Asleep at the Wheel, Terminator, Son of Terminator, Khyber Pass, and Himalayas. Their current marker stations are order-interpolated review scaffolds, not exact locations. Guide/GPS/aerial stationing, rapid-window geometry, validated C++ water, simulator execution, and video evidence remain required.

The Poly Haven Namaqualand Cliff 02 import is hash-verified, publisher-scale checked at about 20.2 m, Nanite-enabled, and regression-tested as an isolated review asset. `RaftSim.CaptureZambeziCliffComparison` now produces paired baseline and transient eight-instance guide-seat/river-eye captures without saving a corridor substitution or changing collision, water, solver, or gameplay authority. Human review rejected promotion: the fragments affect only 1.92 percent of guide-seat pixels and 0.94 percent of river-eye pixels, read as dark pasted-on rocks against the smooth gorge approximation, and are not Batoka basalt authority. The asset remains under `ExternalReview`; the next terrain intake must use source-matched jointed-basalt modules or the coverage-unknown Vol.3/full-reach stereo acquisition path.

The project-owned Batoka geology track now has an explicit regional morphology and authority contract. The [Zambezi River Authority project overview](https://www.zambezira.org/sites/default/files/BGHES_Project%20Overview%20Document.pdf) records an approximately 1,000 m Karoo-basalt lava pile, steep-sided incised gorges, nearly vertical structural trends, stacked scarps and ledges, highly jointed middle scarps, irregular brecciated upper scarps, gullies, boulders, and blocky or gravelly talus. The [ERM cultural-heritage study](https://www.erm.com/contentassets/6bdbb76b347f4e9fb9b9c14054806210/annexes/annex-m---zam-cultural-heritage-report.pdf) independently records dark fine-grained basalt on both sides of the gorge, shallow or stripped soil, talus creep and fluvial incision, resistant basalt ridges, and brecciated basalt decaying to granular rubble and dark soil. These are regional authoring constraints, not surveyed geometry for Boiling Pot to Mukuni Beach. `zambezi_batoka_basalt_authoring_manifest.json` therefore forbids the generated modules from replacing source terrain, centerline, banks, bed, collision, custom C++ water, GeoClaw, forcing, raft contact, or gameplay authority.

Ten isolated Batoka iterations are preserved rather than quietly overwritten. V1 proves the deterministic four-module, Nanite, saved-map, and sixteen-view pipeline but fails as disconnected slabs with perforated joints. V2 replaces those slabs with connected walls and uses a bounded two-sided diagnostic to localize a missing face to winding/culling; V3 fixes the one-sided winding but retains detached bright shelves, cubic talus, crushed material response, and a weak silhouette. V4 removes detached shelf blocks and replaces cubes with angular talus, but discrete depth jumps and terrain-oriented normals produce a dark chaotic faceted mass. V5 introduces smooth drifting joints, cumulative flow setbacks, reduced relief, and cliff-specific normals. Its connected wall is readable, but regular flutes, poured smooth shading, overexposure, wedge-like transitions, and a flat detail-free surface still fail the closeup and 60 m art gates.

V6 through V8 close the first surface and geometry failures. V6 imports the hash-reviewed CC0 ambientCG `Rock037` maps under an explicit generic-rock-only boundary, but a 2.4 m footprint repeats as masonry across small hard panels. V7 expands the projection to 12 m, adds irregular macro relief, and removes crest closures, but hard per-panel normals create a rectangular lighting mosaic. V8 switches to a denser shared-vertex surface with coherent cliff normals; the wall becomes readable, but Rock037 still repeats directionally at cliff scale and cannot establish Batoka lithology.

V9 imports the separately hash-reviewed CC0 Poly Haven `Aerial Rocks 02` map family. The publisher records a 50 m natural-rock surface at 0.8 px/cm for the selected 4K files. Mapping it once across each module removes obvious macro repetition and passes the 60 m central-face review, but it is visibly soft in closeup and is an aerial generic-rock source with vegetation, not a Batoka capture. V10 retains that unique 50 m macro pattern and adds only a bounded Rock037 detail blend: 16 percent color at 1.18 scale, 42 percent detail normal, and 28 percent detail roughness over a 2.4 m footprint. The exact-camera evidence now holds in the central face at close and 60 m distances without the V6 wallpaper or V7 polygon bands.

V10 is conditionally accepted only for a transient non-colliding corridor comparison. It is not production, photoreal, geology, guide, geospatial, performance, collision, water, forcing, raft-contact, or gameplay accepted. Exposed module perimeters and talus remain review artifacts; the corridor experiment must hide those perimeters inside visual terrain, compare paired guide-seat and river-eye frames against the untouched baseline and rejected Namaqualand control, reject pasted-fragment silhouettes or hazard occlusion, and save no source-map substitution. Exact Batoka lithology and named-outcrop identity remain blocked pending reach-specific evidence and geologist review. Every V1-V10 report and capture set is hash-locked by `zambezi_batoka_basalt_review.py`.

Two transient V10 corridor placements are now preserved and rejected. C1 proves that 0.72-1.02x modules are only 20-30 m visual patches against the much larger corridor scarps; they hover away from the sampled slope and read as pasted rectangles. C2 scales the same source-bounded modules to 2.8-4.4x, aligns each actual front plane to the sampled Landscape point, and buries the base by 10 m. This reaches an 84-132 m gorge envelope and demonstrates that the V10 material remains readable under corridor lighting, but vertical sheet seams, overlaps, oversized talus shadows, and the untouched smooth orange 30 m DSM approximation remain obvious. Both commands create transient collision-off actors, save no map substitution, and leave all physics authority unchanged. The result closes standalone-sheet placement as a useful path: the next Batoka implementation must integrate the retained two-scale response into continuous visual terrain and cliff morphology, then address higher-resolution terrain, gorge woodland, water, mist, lighting, and hazard readability.

V11 completes and rejects the first continuous-terrain material experiment. `RaftSim.CaptureZambeziBatokaTerrainIntegratedComparison` creates a separate two-scale review material, loads the saved physical corridor, and transiently overrides only the four dense render-only terrain tiles before taking exact guide-seat and river-eye frames. The saved map, source Landscape, height query, collision, centerline, water, GeoClaw, forcing, raft contact, hazards, and gameplay authority are unchanged. This removes the visible module perimeters and proves that continuous visual-terrain authoring is the correct ownership boundary, but the visual result still fails: top-down UVs stretch the 50 m macro and 2.4 m detail textures into long vertical streaks on steep banks, while the 30 m DSM remains rounded and cannot express stacked scarps, ledges, flow breaks, joints, gullies, or blocky talus. `zambezi_batoka_v11_terrain_integrated_visual_review.json` hash-locks the rejection. The next pass must use tri-planar or world-aligned projection for all rock channels and replace or condition the coarse terrain with reviewed higher-resolution morphology before woodland, water, mist, lighting, and hazard-readability work can be judged honestly.

V12 closes the projection defect without promoting the scene. `RaftSim.CaptureZambeziBatokaWorldAlignedTerrainComparison` maps macro color, normal, roughness, and AO plus detail color, normal, and roughness through Unreal's world-aligned functions at the same exposed 50 m and 2.4 m scales; the review material uses world-space normal output. Exact-camera evidence shows consistent scale across differently oriented banks and removes the worst V11 top-down stretching. `zambezi_batoka_v12_world_aligned_visual_review.json` retains that graph only as the technical projection basis. The environment remains rejected because the 30 m DSM and 12.5 m render sampling produce rounded masses rather than stepped scarps and talus, the generic brown source is not Batoka lithology, and foliage, water, atmosphere, and lighting remain blockout quality. Further material-weight tuning is now prohibited as a substitute for morphology: higher-resolution reviewed terrain is the next gating input.

V13 tests and rejects the remaining bounded procedural fallback instead of silently enlarging it. `RaftSim.CaptureZambeziBatokaVisualMorphologyComparison` keeps the V12 material, protects the first 220 m around the source centerline with a fade to 650 m, and applies render-only elevation-conditioned flow breaks, two irregular joint families, and talus-scale breakup under a 4.5 m absolute cap. Each comparison accounts for all 1,631,500 visual vertices: 76,403 are river-protected, 1,471,404 fail the slope mask, and 69,987 move by a mean absolute 29.35 cm with -3.94 m/+2.93 m extrema. The exact cameras change 5.22 and 7.84 percent of pixels, but mean channel deltas are only 0.19 and 0.25 levels and the rounded silhouette is effectively unchanged. `zambezi_batoka_v13_visual_morphology_visual_review.json` therefore rejects the pass. Increasing displacement enough to fabricate scarps would invent unsupported walls and banks, so the next legitimate input is rights-cleared higher-resolution terrain: feasibility Vol.3 only if its coverage index intersects the game reach, or a commercially licensed stereo/field-capture product with full reach coverage and complete datum, accuracy, derivative-use, and attribution metadata.

The project-owned Futaleufu coigue family now has a separate far-corridor runtime representation and a 512-tree HISM review map. The full eight-form closeup family remains intact at 350.87 MiB; deterministic one-in-six far-card meshes plus the structural trunks reduce the final runtime far family to 19.33 MiB, 435,064 vertices, and 223,592 triangles, a 94.492 percent resource reduction. The corrected benchmark keeps crowns registered to their trunks, but it remains a technical candidate: the neutral stand is too dark and regular, has no mixed native ecology or corridor masks, and lacks near/mid/far transition, wind, packaged desktop, and on-device VR evidence. `futaleufu_native_canopy_coigue_v12_runtime_lod_review.json` keeps corridor substitution and production promotion closed.

The V13 pass tests that retained LOD and placement path in the real Futaleufu physical corridor rather than another neutral stand. `RaftSim.CaptureFutaleufuNativeCanopyCorridorComparison` maps the source vegetation and water masks over the physical Landscape, samples authoritative terrain height, enforces a 65 m riparian setback, 15 m spacing, 100-900 m elevation and 39-degree slope bands, moist-aspect thinning, and deterministic macro/micro canopy gaps. It places 1,200 transient non-colliding trees per capture through all eight forms and three representation bands while hiding only the four temporary PVE foliage actors; the saved map, collision, dense visual terrain, river ribbon, water, rocks, custom C++ solver, and GeoClaw reference remain unchanged. The guide-seat view records 50 full, 126 intermediate, and 1,024 runtime-far trees; river-eye records 40, 157, and 1,003.

That technical placement success fails the visual gate. Paired captures reduce green-dominant image coverage from 0.5098 to 0.0011 percent in guide-seat and from 0.3582 to 0.0349 percent in river-eye. The replacement reads as bare dark trunks, black crown or shadow clumps, and empty slopes, while smooth 30 m terrain and provisional water remain dominant. `futaleufu_native_canopy_coigue_v13_corridor_visual_review.json` therefore retains the source-masked HISM pipeline and V12 runtime-far LOD only; it rejects coigue corridor and production promotion. The next pass must persist explicit instanced-material usage, isolate the leaf opacity/mip/transmission/shadow failure under corridor lighting, preserve green crown mass at 300-1000 m, add a second ecology-reviewed native canopy species plus riparian understory/deadwood strata, and then repeat moving-camera and packaged desktop/VR gates.

V14 closes the material-instancing order question without improving the image. Bark and leaf packages now explicitly enable `InstancedStaticMeshes`, finish compilation, and save before any HISM is created. The repeated guide-seat and river-eye comparison captures are byte-for-byte identical to V13, with zero changed pixels and zero mean RGB delta. `futaleufu_native_canopy_coigue_v14_instanced_material_visual_review.json` therefore retains the packaging/runtime-readiness correction but rejects late shader-permutation creation as the cause of the black crowns and ground smears. The next controlled pass must keep camera, exposure, geometry, masks, and placement fixed while separating leaf-shadow behavior, opaque card visibility, masked opacity/mips, two-sided transmission, and distance-crown geometry.

V15 localizes two independent failures. Turning off only native leaf shadows removes the elongated black ground carpets, but leaves the crowns almost completely invisible. Replacing only the leaves with an opaque two-sided green diagnostic material then reveals dense crown geometry correctly registered to trunks in the near, mid, and runtime-far bands. All four captures retain identical 1,200-tree placement counts, and the saved map plus physics authority remain unchanged. `futaleufu_native_canopy_coigue_v15_leaf_shadow_opacity_visual_review.json` therefore retains the HISM placement and crown meshes, rejects both native masked promotion and the intentionally non-photoreal opaque control, and directs V16 to split alpha/mip/masked-cutoff behavior from TwoSidedFoliage lighting and transmission with leaf shadows held off.

V16 proves that opacity loss and dark foliage response are sequential defects. The live Unreal texture has alpha, preserves it through compression, uses Sharpen5 plus 0.5 alpha-coverage mip scaling, never streams, and does not use virtual texturing. Under the same masked TwoSidedFoliage shader, `LeafOpacityScale=4` restores the full near/mid/runtime-far silhouette, but it differs from constant opacity in fewer than 0.05 percent of pixels and both render as nearly black solid crown masses. The source atlas itself averages about `(82,112,48)` over opaque leaves, so source RGB is not the darkness cause. `futaleufu_native_canopy_coigue_v16_same_shader_opacity_visual_review.json` keeps the new parameters at neutral defaults, rejects both overrides, and directs V17 to isolate AO, sampled normals, and lighting response before reducing opacity amplification to a leaf-shaped distance solution.

V17 excludes AO and sampled leaf normals as the primary corridor-darkness cause. Holding the 4x restored-alpha silhouette, leaf-shadow disable, placement, camera, exposure, terrain, and physics boundaries fixed, AO-off is byte-identical to the guide-seat reference and effectively identical in river-eye; flat normals change fewer than 0.03 percent of pixels. A bounded `LeafDiagnosticEmissive=0.35` mode makes the stored olive source color respond, but remains deliberately non-physical and promotion-ineligible. `futaleufu_native_canopy_coigue_v17_lighting_visual_review.json` retains neutral `LeafNormalStrength=1` and `LeafDiagnosticEmissive=0`, rejects every V17 mode, and directs the next diagnostic to compare the accepted isolated-tree front/back fill with a separately recaptured stronger skylight before any production photometry or transmission change.

V18 rejects global corridor brightness as the primary foliage correction. A fresh alpha4 baseline reproduces the V16 hashes exactly. The transient isolated-review 0.75/0.38 front/back fill changes 43.36 percent of guide-seat pixels and 30.17 percent of river-eye pixels, so the lights are active, but a mask isolated from the V15 native-opacity reference records only a 2-3 level crown RGB lift and remains 70.86/82.68 percent dark. A separately recaptured 3.10 skylight is byte-identical to baseline under this offscreen path. `futaleufu_native_canopy_coigue_v18_corridor_light_rig_visual_review.json` keeps saved photometry unchanged and directs V19 to split `LeafBaseColorScale` from `LeafTransmissionTint` under fixed lighting before reducing the opacity diagnostic.

V19 confirms that source reflectance contributes to the dark response but is not the whole defect. Doubling `LeafBaseColorScale` raises crown-mask green classification from 0.88 to 19.04 percent in guide-seat and 1.85 to 29.68 percent in river-eye; neutral-white transmission alone is minor. Combining both reaches 28.33/37.46 percent green classification, yet still leaves 43.91/71.78 percent of restored-crown pixels dark and looks near-black at gameplay distance. `futaleufu_native_canopy_coigue_v19_reflectance_visual_review.json` retains neutral production defaults, rejects all overrides, and requires a same-texture masked DefaultLit versus TwoSidedFoliage comparison before any physical material correction or opacity reduction.

V20 retains native TwoSidedFoliage. A same-texture masked DefaultLit diagnostic at both 1.18 and 2.36 reflectance is darker and substantially less green than TwoSidedFoliage, especially in river-eye, so the shading model is not the primary defect. Source inspection instead exposes transparent-black mip contamination: the generator explicitly zeros RGB wherever alpha is at most 2/255, while Unreal preserves alpha coverage in distant mips. `futaleufu_native_canopy_coigue_v20_shading_model_visual_review.json` rejects DefaultLit and requires deterministic per-tile RGB padding that preserves alpha and opaque RGB byte-for-byte, followed by a native-material corridor recapture before opacity reduction.

V21 fixes the dominant corridor color-loss defect at source. The deterministic generator pads 517,669 transparent texels up to 24 pixels within each 4x4 atlas tile while preserving the alpha hash and all nontransparent RGB byte-for-byte; the normal and packed maps remain unchanged. After Unreal reimport, neutral native TwoSidedFoliage crown-mask green classification rises from 0.88/1.85 percent to 65.45/71.00 percent in guide-seat/river-eye, while dark coverage falls from 78.21/85.07 percent to 31.47/61.34 percent. `futaleufu_native_canopy_coigue_v21_mip_padding_visual_review.json` retains the source fix but rejects visual promotion because alpha4 still produces solid card masses. The next pass must compare alpha scales 2 and 3, choose the lowest continuous silhouette, and only then revisit bounded leaf shadows.

V22 selects alpha2 as the lowest current corridor candidate. Alpha2 and alpha3 are byte-for-byte identical in both views, while alpha2 keeps visible near/mid/runtime-far crowns, restores leaf-shaped olive breakup, raises crown means to about `(98,118,83)` and `(97,116,79)`, and reduces dark crown coverage below one percent. `futaleufu_native_canopy_coigue_v22_opacity_selection_visual_review.json` retains the material default at 1.0 and keeps alpha2 review-only. The next gate must reintroduce dynamic leaf shadows with contact and distance-field indirect effects disabled, compare against the no-shadow alpha2 reference, and reject any recurrence of V15's elongated ground carpets before ecology expansion.

V23 proves that bounding shadow reach and preserving usable crown response are separate gates. Alpha2 leaf shadows are limited to the near representation; static, contact, distance-field, dynamic-indirect, mid, and runtime-far leaf shadow effects are explicitly disabled. The V15 off-crown ground-smear signal drops from 1,714/2,163 guide-seat/river-eye pixels to 2/1 pixels, so elongated valley-floor projection is effectively removed. The same mode still self-darkens affected crowns: dark coverage rises to 36.26/42.17 percent and green classification falls below one percent. `futaleufu_native_canopy_coigue_v23_bounded_shadow_visual_review.json` therefore rejects the mode, retains alpha2 no-shadow only as the next ecology-expansion reference, and requires a later shadow-pass-density or simplified shadow-proxy treatment to pass both the off-crown artifact metric and crown-photometry gate before production promotion.

V24 proves that route-wide density was only one part of the empty-valley failure. `RaftSim.CaptureFutaleufuDenseCanopyComparison` holds the V22 alpha2 no-shadow material, eight coigue forms, runtime-far representation, source masks, terrain, water, lighting, and cameras fixed while comparing the retained 1,200-tree route-wide strip with a transient 9,000-tree camera-local profile. Both dense views complete, changing 4.77/6.50 percent of guide-seat/river-eye pixels, but the result is not a forest: centerline-progress sampling creates contour-following rows, hard stand edges, and exposed trunk fences. Whole-image green classification does not materially improve while dark coverage rises from 2.66/3.90 to 5.56/7.59 percent. `futaleufu_native_canopy_coigue_v24_dense_canopy_visual_review.json` rejects production promotion and retains only the measured local workload. The next ecology pass must use deterministic two-dimensional area or blue-noise sampling over feathered source masks, irregular density fields and stand gaps, mixed age/species and understory strata, and representative terrain, water, boulder, mist, and atmosphere treatment before another corridor gate.

V25 validates that first topology correction without promoting the scene. `RaftSim.CaptureFutaleufuAreaSampledCanopyComparison` compares the unchanged V24 centerline-dense baseline against direct deterministic two-dimensional candidates in a 2.2 km camera-local square, true nearest-centerline setback, and broad/mid-scale stand fields. Both area-sampled views reach 9,000 trees; the strongest contour-following rows and trunk fences are reduced, and 10.58/8.28 percent of guide-seat/river-eye pixels change. The result still fails the visual gate: a mostly binary vegetation mask produces broad hard forest bands, one coigue stratum remains repetitive, near/mid allocation is too low, and coarse terrain, bare ground, flat water, sparse boulders, and dry atmosphere dominate. `futaleufu_native_canopy_coigue_v25_area_sampled_visual_review.json` retains the 2D sampling and measured nearest-river-distance contract but rejects corridor and production promotion. The next pass needs a spatial mask distance field, camera-independent world-cell streaming, mixed native ecology, representative terrain/water/rock/atmosphere, and moving-camera performance evidence.

V26 establishes camera-independent forest identity, but not a finished forest. `RaftSim.CaptureFutaleufuWorldStableCanopyComparison` replaces each camera's local square with one fixed 6.0 km by 2.2 km curvilinear corridor domain, raises the target to 24,000 to preserve approximate area density, and derives a 512-cell chamfer signed-distance occupancy field with a 180 m feather. An initial straight-domain implementation missed the bent guide-seat reach and was corrected before evidence was retained. The final guide-seat and river-eye runs each evaluate 148,909 candidates, accept the same 24,000 transforms, and share the exact `f30715fa74aee851` placement fingerprint while selecting camera-specific LOD representations. This is a real streaming/topology prerequisite, but the visual settings are rejected: scattered feather-edge patches join the existing hard bands, photometry improves in one view and regresses in the other, and the single-species/coarse-terrain/flat-water scene remains non-lifelike. `futaleufu_native_canopy_coigue_v26_world_stable_visual_review.json` retains the curvilinear coordinate, fingerprint, and occupancy-field mechanisms while rejecting the current 180 m feather, density response, corridor promotion, and production promotion.

The second Futaleufu canopy stratum is now selected from local authority rather than visual convenience. CONAF records `Austrocedrus chilensis` in near-pure north/east-facing groves at Futaleufu, MMA SIMBIO lists an `Austrocedrus chilensis - Nothofagus dombeyi` evergreen association in the watershed, and INFOR supplies the 400-1,500 m range plus tree, bark, and millimeter-scale leaf morphology. `futaleufu_cordillera_cypress_authoring_manifest.json` separates those facts from bounded review heuristics: initial placement is limited to verified north/northeast/east aspects at 400-900 m, clustered groves remain at most 18 percent of review canopy, wet riparian and unverified bands are excluded, and a true-north transform is mandatory. The project must author five adult and three intermediate forms using scale-leaf branch sprays and alpha-safe texture padding, then pass isolated visual, rights, corridor, temporal, desktop, VR, and local ecology gates before any production claim.

The first cordilleran-cypress texture stage is now reproducible and rights-bounded. A deterministic project-owned generator produces periodic warm brown-to-ash bark and a sixteen-tile scale-leaf branch-spray source, then derives six 2048-pixel Unreal candidates. The spray atlas applies 24 pixels of tile-bounded transparent-RGB padding while preserving alpha and nontransparent RGB; source, generator, padding, and output hashes are retained in `futaleufu_cordillera_cypress_texture_manifest.json`. No external reference pixels or geometry are copied, and the manifest explicitly distinguishes generated appearance studies from botanical measurements. These are technical candidates only: production and corridor use remain forbidden until the five-adult/three-intermediate Unreal family passes isolated Metal closeup, turntable, 60 m, and 150 m review.

Cordilleran-cypress V1 proves the complete Unreal family path but fails the visual gate. `RaftSim.CreateFutaleufuCordilleraCypressFamily` imports the six textures, builds explicit bark and TwoSidedFoliage spray materials, saves sixteen independent review meshes for five adult and three intermediate forms, and captures forty offscreen Metal views with spray shadows disabled. The form dimensions and seeds are distinct, but the result is not a usable evergreen: turntables and distance views read as bare poles with evenly spaced exposed branches, while closeups reveal flat intersecting cards and smooth cylindrical joints. The hash-locked review measures mean green-dominant coverage at only 0.081 percent in turntables, 0.189 percent at 60 m, and 0.076 percent at 150 m. `futaleufu_cordillera_cypress_v1_visual_review.json` therefore retains the pipeline and rejects V1 without corridor substitution. V2 must create substantially denser source-matched feathery sprays, continuous layered crown mass, blended branch junctions, and less regular cadence, then repeat all forty views and clear explicit visibility floors before human review.

Cordilleran-cypress V2 separates distance visibility from closeup fidelity. A versioned project-owned atlas raises alpha coverage from 6.5 to 21.6 percent, while larger layered cards, additional branch sprays, and overlapping primary collars preserve the eight form identities. Forty new fixed-camera Metal captures show a large but bounded gain: mean green-dominant coverage increases 15.49x in turntables, 10.74x at 60 m, and 14.86x at 150 m. The 60 m and 150 m floors pass; turntables reach 1.26 percent and miss the 1.5 percent floor. More importantly, the closeup gate rejects the method regardless of those gains: dense tiles read as broad opaque brush-like slabs with crossed planes and hard silhouettes, while exposed branch tiers and cylindrical joints remain. `futaleufu_cordillera_cypress_v2_visual_review.json` retains only the distance-density lesson and keeps production and corridor promotion closed. V3 must replace fan slabs with smaller irregular feathery sub-sprays on explicit tertiary directions, build offset crown volume and lower/middle ramification, and repeat the full forty-view gate.

Cordilleran-cypress V3 tests whether explicit ramification can solve V2's card-wall failure. Every form now builds modeled primary, secondary, tertiary, and interior shoots with 950-1,808 individually placed irregular sub-sprays, and all forty fixed-camera Metal captures complete without material, shader, asset, map, or fatal errors. The broad fan slabs are gone, but the family still fails. Mean turntable green coverage drops to 0.71 percent, only 56.19 percent of V2 and below the 1.5 percent floor; 60 m and 150 m means retain their minimum floors at 1.26 and 0.64 percent but lose roughly forty percent of V2's visible mass. More importantly, closeups expose fern-like planar strips with repeated orientation, hard silhouettes, and long smooth intersecting shoots, while full-tree views again read as sparse exposed tiers. `futaleufu_cordillera_cypress_v3_visual_review.json` hash-locks all forty rejected views and keeps the real corridor untouched. V4 must assemble short dense scale-leaf clusters around tapered terminal shoots, selectively restore crown overlap without V2 slabs, improve woody junction breakup, and pass both quantitative and independent closeup/silhouette gates.

Cordilleran-cypress V4 proves that card multiplication can recover distance mass but cannot solve the representation. Each old terminal card becomes five short tapered shoots with two rolled sub-sprays, producing 12,380-23,600 cards and 58,260-110,928 authored woody vertices per form. Forty clean Metal captures improve mean turntable, 60 m, and 150 m green coverage by 1.53x, 1.40x, and 1.66x over V3. The distance floors pass, but turntables reach only 1.09 percent against the 1.5 percent floor and remain below V2's density. Human review is decisive: full-tree crowns still read as exposed horizontal tiers with isolated dark tufts, while closeups become overdraw-heavy near-black tangles of repeated planar silhouettes and needle-thin crossing shoots. `futaleufu_cordillera_cypress_v4_visual_review.json` preserves all forty hashes and rejects corridor promotion. V5 must change crown topology and representation together: irregular source-matched axial branch groups, connected geometric near sprays, a reduced registered mid/far silhouette, and explicit masked-overdraw and resource gates.

Cordilleran-cypress V5 separates near fidelity from distance cost at a deterministic 28 m handoff. Closeups render an opaque connected five-shoot representation; turntable, 60 m, and 150 m views render only three registered masked cards per terminal cluster. The far asset uses 3,930-7,494 cards per form, 68.25 percent fewer than V4, while mean turntable and 150 m green coverage improve 1.20x and 1.33x. That distance split is retained, but the visual gate still rejects the family. Turntables reach 1.31 percent against the 1.5 percent floor, horizontal tiers remain obvious, and closeups expose large faceted green spikes with blunt bases, pointed tips, flat color, and heavy intersections instead of millimeter-scale cypress foliage. `futaleufu_cordillera_cypress_v5_visual_review.json` hash-locks all forty views and leaves the corridor untouched. V6 must replace spikes with connected curved spray ribbons or authored branch meshes, retain the lighter far silhouette, rebuild lower/middle axial crown groups, and add explicit 20/28/36 m handoff evidence.

Cordilleran-cypress V6 replaces V5 spikes with connected curved main and side ribbons and expands the gate to 49 views: the original forty plus 20/28/36 m handoff captures for two adults and one intermediate. Mean turntable, 60 m, and 150 m coverage improve 1.39x, 1.64x, and 1.27x over V5, clearing every visibility floor for the first time, and the handoff sequence retains registered foliage mass. Those gains are not sufficient for acceptance. Extreme closeups expose broad rectangular ribbon faces, hard cut ends, flat green material, and dense intersections; near assets cost 235,800-449,640 vertices and 366,800-699,440 triangles per form. More importantly, unchanged far meshes produce materially different fixed-distance coverage from V5, so bounds-based component culling may overlap representations and the current handoff images do not prove authority. `futaleufu_cordillera_cypress_v6_visual_review.json` retains the floor pass but rejects promotion. V7 must capture explicit near-only, far-only, and combined modes, replace broad ribbons with compact textured fronds, reduce near triangles by at least 75 percent, and rebuild lower/middle crown topology.

Cordilleran-cypress V7 turns the suspected LOD overlap into measured evidence. It replaces each ten-ribbon V6 cluster with one main and two side ribbons, reducing near triangles 77.14 percent to 83,840-159,872 per form, and expands the capture gate to 67 images: forty fixed plus near-only, far-only, and combined authority modes at 20/28/36 m for three forms. The first run exposed and fixed a stale-world-pointer crash by moving visibility overrides into the capture helper's freshly loaded-world callback. The corrected run proves independent control, but rejects the production handoff: combined matches far-only in only one of nine triplets, never matches near-only, and differs from both in eight, proving bounds-based culling overlaps representations. Far-only supplies the usable crown mass; near-only remains sparse, dark, jagged, flat, and untextured. `futaleufu_cordillera_cypress_v7_visual_review.json` preserves all hashes and pixel deltas. V8 must use actor-root distance authority so combined selects exactly one representation outside a deliberate transition band, and near foliage must remain disabled until a genuinely higher-fidelity textured branch-spray asset exists.

Cordilleran-cypress V8 closes the representation-authority defect without pretending that it closes the visual defect. The capture callback now resolves each freshly loaded actor pair and selects visibility from camera-to-actor-root distance; because the V7 near representation failed review, an explicit eligibility flag forces combined mode to the retained far silhouette at every distance. Across 27 handoff captures, all nine combined images are byte-identical to far-only, none match near-only, and none mix the two representations. The forty fixed views preserve the accepted turntable, 60 m, and 150 m visibility floors, but far-only closeup green coverage falls from V7's 18.92 percent to effectively zero and the surviving masked cards still look synthetic. `futaleufu_cordillera_cypress_v8_visual_review.json` therefore accepts actor-root authority, retains the far fallback, and keeps near foliage, production promotion, and corridor placement disabled. V9 must provide genuinely high-fidelity textured branch sprays and better lower/middle crown topology; hard near selection must pass before any narrow deterministic transition, temporal, ecology, desktop, or VR gate is attempted.

Cordilleran-cypress V9 improves the source image but rejects the representation assembled from it. A project-commissioned generated terminal spray, created without supplied reference images, is chroma-keyed locally and deterministically expanded into a sixteen-variant 2048-pixel atlas with alpha-safe mip padding, normal, and packed surface maps. Eight textured cards per terminal cluster cut near geometry 75 percent from V8 to 20,960-39,968 triangles per form, and near-only closeups finally resolve individual scale leaves, twig color, and asymmetric spray detail. The full gate still fails. Those clusters produce 10,480-19,984 masked cards per form, closeups show severe radial self-intersection and dark overdraw pockets, and 20/28/36 m near views retain only 49-69 percent of far foliage coverage while edge-on cards read as pointed spikes. Because V9 temporarily shared its narrow atlas with far cards, authoritative turntable and 60 m coverage also regress below their retained floors. `futaleufu_cordillera_cypress_v9_visual_review.json` hash-locks all 67 captures and accepts only the texture-detail lesson plus exact far-fallback authority. V10 must separate near/far textures, use several independently authored broad flattened sprays, reduce card overlap, rebuild lower/middle crown topology, and keep near ineligible until it meets or exceeds far coverage at 20 m.

Cordilleran-cypress V10 closes the coverage and resource defects without passing the photoreal topology gate. Three independently generated, project-owned broad spray sources feed a deterministic sixteen-tile 2048-pixel near atlas, while the accepted V3 distance atlas remains isolated in a separate far material. Three source-aligned cards per cluster reduce near assets to 3,957-7,545 cards and 7,914-15,090 triangles per form, 62.25 percent fewer cards than V9's maximum. The old green-only handoff ratio is now diagnostic only because its result changes with source color. A fixed-background foreground segmentation instead measures the complete tree silhouette under identical cameras, lighting, woody geometry, and background; all nine 20/28/36 m pairs pass with near/far ratios of 1.033-1.094. All nine combined captures remain byte-identical to far-only, so authority is still exact. `futaleufu_cordillera_cypress_v10_visual_review.json` nevertheless rejects production and corridor use: closeups expose repeated whole-spray planes, dark overlap pockets, and disconnected-looking twigs, while full crowns remain coarse horizontal shelves. V11 must rebuild grouped curved primary systems, use one or two branch-aligned broad cards rather than radial clumps, preserve the new silhouette and resource floors, and repeat the 67-view gate before near authority is enabled.

Cordilleran-cypress V11 and V12 bound the remaining masked-card option and close that line of retuning. V11 groups and pitches primary branches, limits spray roll, uses two cards per terminal cluster, and moves closeups onto exterior attachments. Cost falls to 2,638-5,030 cards, but the grouped branches produce even clearer shelves and minimum silhouette regresses to 0.964. V12 restores the V10 primary distribution and treats each generated image as a complete branch spray, selecting one enlarged card on only 34 percent of terminal systems. That reaches just 443-945 cards and 886-1,890 triangles per form, and individual sprays read at a more plausible scale, but every handoff pair fails the silhouette floor at 0.815-0.926. Exterior closeups still show stretched intersecting photographs and disconnected attachments. `futaleufu_cordillera_cypress_v11_visual_review.json` and `futaleufu_cordillera_cypress_v12_visual_review.json` hash-lock both rejected endpoints. More of the same cards would trade back toward V10 overdraw rather than create connected scale-leaf topology.

The next cypress step is a morphology-donor gate, not V13 card density. An exact `Austrocedrus chilensis` model was not found in the initial Fab, SpeedTree, or general commercial search. The workspace already contains three rights-reviewed CC0 Poly Haven `Fir Tree 01` Nanite meshes with 0.5-4.2 million source triangles, explicit material slots, and plausible 14-19 m heights. They remain a non-native fir and cannot be placed or labeled as cypress, but their connected branch hierarchy can be reviewed as an open-source geometry donor. A photoreal Western Red Cedar Fab model is the strongest link-only Cupressaceae alternative because it shares flattened scale-leaf sprays; generic photogrammetry cypress packs are secondary comparisons. V13 must isolate these donors, preserve provenance and no-purchase boundaries, compare them against V10/V12, and either define a reconstruction path to the native source contract or pivot to opaque geometric branchlets and scale-leaf clusters.

Cordilleran-cypress V13 closes the direct donor question and exposes a reusable import warning. `RaftSim.CreateFutaleufuCordilleraCypressDonorReview` loads the three already reviewed CC0 Poly Haven fir meshes with their original materials, verifies Nanite and 14.1-18.9 m effective height contracts, saves a separate local map, and captures twelve fixed Metal views with exact camera-label checks. The legacy imports have `BuildScale3D=100` while persisted component bounds remain pre-build-scale, so correctly sized Nanite geometry disappears at actor scale one unless the isolated review expands culling bounds; actor scale 100 instead makes the rendered geometry kilometer-scale. V13 records a review-only 220x bounds override without mutating the source assets. The resulting views are decisive: 6.98 million source triangles produce connected, irregular wood, but the original needle stratum collapses and all three trees read as bare fir snags. Mean green-dominant coverage is only 0.000705 percent in turntables and 0.000723 percent at 60 m. `futaleufu_cordillera_cypress_v13_morphology_donor_visual_review.json` therefore rejects direct reuse, native identity, corridor placement, and production promotion. V14 must author project-owned opaque connected branchlets and scale-leaf clusters, preserving only the general connected-wood lesson plus V10/V12/V13 quantitative guardrails; the paid Western Red Cedar and generic cypress listings remain link-only and unpurchased.

Cordilleran-cypress V14 tests that opaque path and rejects the inherited card budget as a production constraint. `RaftSim.CreateFutaleufuCordilleraCypressOpaqueNearReview` authors eight project-owned forms with welded three-lobe opaque scale-leaf clusters attached at branch systems, copies no donor geometry, uses no masked whole-spray cards, preserves the V10 far fallback, and repeats all 67 exact-camera Metal captures. A deterministic 11 percent sample keeps each form at 139-305 clusters and 834-1,830 near triangles, inside the V12 envelope. That mechanical success is visually conclusive in the wrong direction: closeups show fluorescent triangular blades, the primary skeleton remains tiered, and near/far foreground silhouette falls to 0.546-0.870 while all nine combined views remain byte-identical to far-only. `futaleufu_cordillera_cypress_v14_opaque_near_visual_review.json` retains the connected-attachment mechanism and exact fallback gate, rejects the geometry, and retires the 1,900-triangle card-derived ceiling for opaque near foliage. V15 must use rounded twig segments and bounded scale-leaf volumes, break the tiered crown, and establish a measured Nanite near-tier budget through packaged desktop and target-VR profiling before corridor placement.

Cordilleran-cypress V15 separates density, morphology, and renderer suitability. `RaftSim.CreateFutaleufuCordilleraCypressVolumetricNearReview` grows connected brown central/secondary twigs plus eight rounded fusiform leaf volumes on a deterministic 50 percent sample, producing 625-1,316 selected branch systems and 160,000-336,896 micro-foliage triangles per form. Fixed turntable, exterior-closeup, and 60 m captures explicitly use near-only authority. The first diagnostic shows that Nanite `PreserveArea` explodes thin open segments into shards; Nanite with shape preservation disabled keeps woody geometry stable, but still culls most tiny disconnected foliage volumes. Moving only micro-foliage to traditional raster reveals the complete density and passes every near/far silhouette floor at 1.273-1.600 while all nine combined captures remain exact far fallbacks. The visual gate still fails: dense faceted pods remain visibly primitive, the crown stays tiered, and closeups have no scale-leaf or material fidelity. `futaleufu_cordillera_cypress_v15_volumetric_near_visual_review.json` therefore retains the Nanite-woody/traditional-micro-foliage split and rejects further primitive-lobe tuning. V16 must use a real vegetation authoring workflow such as Unreal Procedural Vegetation or SpeedTree, or an explicitly licensed Cupressaceae morphology source, before another isolated gate.

Cordilleran-cypress V16 establishes that real authoring workflow without promoting unfinished art. `RaftSim.EvaluateFutaleufuCordilleraCypressPveCandidate <form_id>` transiently adapts Unreal 5.8's installed Procedural Vegetation sample graph, replaces both sample foliage palettes with project-owned V10 cypress live sprays and connected dead twigs, bypasses the sample-specific manual edit, redirects all derived assets to a git-ignored local review namespace, and records explicit branch, axillary-growth, graft, senescence, direction, density, scale, and seed controls. All five adult and three intermediate forms generate successfully, spanning 27.29-32.45 m and 10.60-19.16 m respectively, with connected Nanite woody geometry using shape preservation `None` and masked micro-foliage using traditional raster rendering. The workflow gate passes, but the visual gate does not: several sprays render too dark, some leaders fork or curl unnaturally, crowns retain coarse shelves/open bands, the spruce scaffold is not native-species approval, and only three diagnostic views per form exist. `futaleufu_cordillera_cypress_v16_pve_visual_review.json` therefore accepts V16 as the production authoring path while rejecting art promotion and corridor placement. The next pass must correct foliage photometry and leader topology, rerun the exact V13 67-camera and V10 far-authority gates, then add mixed coigue-cypress ecology and complete moving-camera, wind, packaged desktop, and target-VR profiling.

The bounded V16 correction closes the material defect but not the morphology defect. The cypress-only material now uses a 2.10 base-color scale, 0.08 AO influence, stronger physical transmission, a 0.42 mask clip, and zero diagnostic emissive; isolated live-foliage shadows are disabled while woody shadows remain. Per-form gravity/angle correction, explicit trunk/branch bifurcation disable, denser retained foliage, graft-angle/phyllotaxy variation, and a deterministic upper-crown spray taper are recorded in every regenerated report and applied identically to local templates and maps. All eight forms regenerate at 27.50-31.08 m adult and 10.40-17.53 m intermediate heights. Foliage is now readable and the false ground-shadow carpet is gone, but the installed spruce scaffold still produces sparse dotted apexes, stacked crown islands, open bands, and coarse shelves. Keep the corrected controls; replace the scaffold with a project-authored Austrocedrus-oriented PVE graph or rights-cleared Cupressaceae morphology source before the exact camera, far-authority, ecology, motion, and platform gates.

Cordilleran-cypress V17 makes that topology replacement. `RaftSim.EvaluateFutaleufuCordilleraCypressPveCandidate <form_id>` now creates a project-owned transient PVE graph for the grower, grafting, gravity, bone reduction, and foliage-distribution path; it duplicates no installed sample topology, preset, manual-edit node, or export node. Only bounded installed mesher and plant-profile settings remain rendering donors, while the live/dead palettes, materials, and authored controls remain project-owned. The first `open_grown_conical` result contains 136,041 vertices, 245,571 triangles, and 3,626 live-spray instances in a 24.68 m by approximately 12 m envelope. The tree has a substantially more continuous pyramidal crown, and the dedicated closeup validates the source spray material and alpha response. It is still rejected: whole-spray card edges remain legible at 60 m, the basal flare needs secondary cleanup, seven forms remain ungenerated, and the exact 67-camera, near/mid/far authority, ecology, temporal, desktop, and VR gates have not run. Keep the project graph, solve distance representation on the open-grown form first, then regenerate and validate the full family before corridor use.

The first V17 distance-representation gate now rejects three bounded paths without weakening that conclusion. Epic's Impostor Baker produces no output after 600 seconds for both 12x12/2048 and 8x8/1024 corrected profiles, then crashes its utility widget during shutdown. A project-owned opaque proxy is structurally valid at 2,721 closed scale-spray wedges and 65,304 triangles, but its exact 60 m image reads as detached geometric shards. A merged Nanite whole tree also fails both shape modes: `PreserveArea` inflates masked spray boundaries into a distorted dark mass, while `None` collapses the foliage into a bare trunk and detached cards. `futaleufu_cordillera_cypress_v17_pve_open_grown_conical_report.json` records the source, proxy, Nanite settings, exact-camera paths, and separate structural versus human decisions. Keep Nanite for connected wood and traditional raster for source micro-foliage; the next unchecked implementation is a project-owned multi-view atlas/HLOD baker, or a separately rights-reviewed distance-stable native asset, with deterministic color/normal/opacity/depth outputs and exact 20/28/36/60 m, temporal, desktop, and target-VR gates.

The project-owned V17 multi-view atlas/HLOD path is now implemented and accepted as a representation mechanism, not as final cypress art. It captures eight azimuths at zero and 25 degrees elevation into deterministic 4x4, 2048-pixel base-color, world-normal, opacity, and depth atlases; records file hashes and bounded coverage; creates local ignored Unreal textures plus a masked unlit camera-facing material; and requires a compiled shader plus the exact source 60 m comparison. Keeping the freshly generated atlases resident removes the first coarse streaming fallback, and correcting the plane-local V convention restores an upright branch-detailed result. The 60 m proxy preserves color and silhouette, but it also faithfully preserves the source whole-spray card edges. Therefore visual/corridor promotion remains rejected pending a better source spray, explicit 20/28/36 m authority, moving-camera temporal review, and packaged desktop/target-VR measurements; a rights-reviewed native distance asset remains a permitted fallback.

A bounded curved-shell palette experiment improves the source geometry without closing the art gate. The command now accepts `flat_cards` or `curved_shells` as a second argument and writes each mode to separate generated asset, capture, atlas, and report namespaces. The paired run holds the V17 PVE graph, seed, 3,626 foliage transforms, three V10 source images, material defaults, cameras, and HLOD baker fixed. Curved mode replaces each three-plane, six-triangle live twig with three six-segment shells at 36 triangles; its exact 60 m frame changes 7.11 percent of pixels above the fixed comparison threshold, dark/chromatic foreground IoU is 0.763, and atlas coverage remains close at 0.153-0.169 versus 0.157-0.172. The branch closeup no longer exposes the baseline's large flat polygon faces, so curved shells are retained as the preferred V17 palette experiment. They are not promoted: each shell still maps one complete broad-spray image, repeated motifs and crown shelves remain, estimated live-foliage cost rises from 21,756 to 130,536 triangles, and the HLOD bakes those source limits. `futaleufu_cordillera_cypress_v17_curved_shell_palette_visual_review.json` hash-locks the paired captures and directs the next pass to connected branch-scale twig decomposition rather than more whole-image shells.

The V18 twig sequence validates that decomposition mechanism but moves the remaining blocker squarely into source art. V18 maps nine smaller branch-scale atlas crops onto curved cards but leaves visible disconnected gaps. V18.1 adds a procedural woody hierarchy, preserves separate bark and foliage material slots through static-mesh conversion, and fills the 60 m crown, but its long lateral connectors protrude through the masked sprays as oversized spikes. V18.2 shortens those laterals while holding the V17 graph, 3,626 foliage transforms, cameras, material defaults, and HLOD baker fixed; its 0.151-0.170 atlas coverage remains stable and its 60 m silhouette stays substantially fuller than disconnected V18. The closeup still exposes repeated planar spray crops, however, and the whole crown retains coarse horizontal shelf rhythms. `futaleufu_cordillera_cypress_v18_twig_hierarchy_visual_review.json` therefore retains V18.2 only as the connected two-material geometry basis and rejects all-form regeneration, corridor substitution, and production promotion.

Rights-reviewed native references reinforce that decision. Six `Austrocedrus chilensis` whole-tree photographs by Krzysztof Ziarnek/Kenraiz under CC BY 4.0 show irregular rounded crown clumps and substantial dead lower wood, while Gagea's CC BY-SA 3.0 branch and leaf photographs show the fine flattened scale-leaf sprays missing from the current near tier. They remain link-only morphology evidence: their natural forest backgrounds do not support a trustworthy automatic alpha cutout, and any pixel-derived use of the share-alike images needs an explicit derivative-license boundary. The next cypress art pass must use a rights-reviewed native or Cupressaceae studio branch source, or authored geometric scale leaves, then repeat the exact 60 m, closeup, and HLOD gate before any 20/28/36 m authority or platform work.

Cordilleran-cypress V19 tests the authored alternative and closes the simple procedural-leaf option. A deterministic project-owned generator creates sixteen scale-leaf tile candidates with no external or reference-derived pixels, while Unreal replaces V18.2's repeated broad spray crops with connected woody shoots carrying explicit masked leaves. The first sparse hierarchy reaches only 0.087-0.093 multi-view atlas coverage. V19.1 increases imbricate density and reaches 0.131-0.146, but the exact closeup still reads as discrete broad lanceolate leaves on a regular comb rather than flattened overlapping cypress scales; its 60 m crown stays noisy and detached, and the unlit HLOD becomes an overly dark mass. `futaleufu_cordillera_cypress_v19_scale_leaf_hierarchy_visual_review.json` hash-locks the V18.2-to-V19 and V19-to-V19.1 source, closeup, and HLOD transitions. Retain the first-party generator boundary, connected-shoot implementation, and Nanite-wood/traditional-raster micro-foliage split, but reject both V19 candidates, all-form regeneration, corridor placement, and production promotion. The next pass requires a rights-reviewed native or Cupressaceae branch source with independent spray variation, or a botanically authored shoot model whose scales pass close inspection before distance authority and platform profiling resume.

Cordilleran-cypress V20 establishes that botanical authored-shoot baseline but does not yet produce acceptable tree art. The source contract cites Argentina's Sistema de Informacion de Biodiversidad and the Gymnosperm Database for flattened planar branchlets, opposite imbricate dimorphic scales, 2-5 mm keeled lateral leaves with pale stomatal bands, and 0.5-2 mm facial leaves. A deterministic first-party generator uses those facts, but no external pixels or geometry, to create sixteen independent 18-23 cm flattened sprays and carries them on connected curved branchlets in Unreal. The exact closeup finally removes V19's broad lanceolate leaf error, and 0.151-0.162 multi-view coverage under the corrected import contract returns to the V18.2 range. The result is still rejected: exposed wood dominates the sparse near spray, the 60 m source becomes skeletal and noisy, and the unlit HLOD is much darker and more opaque than the source. `futaleufu_cordillera_cypress_v20_botanical_spray_visual_review.json` retains V20 as the factual morphology/source-ownership baseline, retains V18.2 only as the density and distance-silhouette benchmark, and rejects corridor, all-form, and production promotion. V20.1 must overlap more measured sprays, reduce exposed-stem rhythm, and correct HLOD color, opacity, and relighting under the same V17 graph, seed, 3,626 transforms, and exact cameras before near/mid/far authority or platform profiling resumes.

Cordilleran-cypress V20.1 closes a hidden texture-residency defect and tests that denser measured source. `BotanicalSpray*` keys had fallen outside the native-leaf import classification because they do not contain `Leaf`, so repeated editor runs disabled full-resolution residency, sharpened foliage mips, and alpha-coverage preservation even though the first freshly imported frame looked correct. The corrected contract keeps those textures resident and scales mips at the material's 0.42 mask threshold. With the V20 tiles fixed, Unreal layers three unchanged sprays on each main branchlet and two on each lateral, increasing dark/chromatic closeup foreground about 65 percent and exact 60 m source foreground about 16 percent. A raw-albedo/world-normal DefaultLit HLOD was explicitly rejected after remaining blue-black; the retained source-lit unlit bake uses the inverse-opacity silhouette and bounded `[1.10, 1.38, 0.90]` color gain. `futaleufu_cordillera_cypress_v20_1_dense_botanical_spray_visual_review.json` accepts those mechanisms but rejects art, corridor, all-form, and production promotion: the near source still exposes regular woody fans and remains skeletal at 60 m, while the HLOD dark-foreground footprint remains about 1.9 times the source and darker. The next source pass must build denser true flattened branchlet mass before any separate 20/28/36 m HLOD coverage/luminance calibration.

Cordilleran-cypress V20.2 tests that connected branchlet-mass change without moving the accepted source or camera boundaries. Each authored twig now carries six staggered connected lateral pairs instead of four, and woody support ends at 0.68 rather than 0.82 of lateral spray length; measured tiles, 3/2 spray layering, seed, 3,626 tree-level foliage transforms, form envelope, HLOD color mechanism, and exact cameras remain fixed. The merged raster source grows from 1.32 million to 1.79 million triangles. Exact-image analysis finds about 11 percent more dark/chromatic closeup foreground, a 2.9 percentage-point reduction in detected wood share, and about 4 percent more source foreground at 60 m. A fresh new material namespace initially exposed gray fallback capture before its second post-usage shader update completed; the helper now forces recompilation, processes async shader results, finishes the material resource, and flushes rendering after enabling instanced-static-mesh usage. Moving the generated V20.2 material aside and rerunning from a clean asset state confirms the corrected first capture renders the measured foliage and alpha. Removing the fallback planes from the bake corrects atlas coverage to 0.096-0.105 and HLOD/source foreground mismatch to about 1.13 times, although HLOD photometry remains darker and 20/28/36 m handoff evidence remains open. `futaleufu_cordillera_cypress_v20_2_branchlet_mass_visual_review.json` excludes the invalid pre-fix run, accepts the clean-run topology and material gate as the next source baseline, and rejects art, corridor, all-form, and production promotion. The next source pass must replace repeated whole-axis fans with hierarchically subdivided flattened shoot clusters and reduce crown tier gaps before HLOD handoff calibration.

Cordilleran-cypress V20.3 records the first hierarchical sub-shoot attempt and rejects its geometry. Under the fixed V20.2 source and camera contract, five side-offset parent pairs replace six pairs, parent lateral spray cards shrink to 0.78 width and 0.82 length, woody support shortens to 0.60, and each lateral receives two connected tertiary sub-shoots with smaller measured spray cards. The entirely new V20.3 material namespace renders correct foliage and alpha on its first capture, directly proving the post-usage shader-resource gate. The art trade is negative: merged raster cost rises from 1.79 million to 2.86 million triangles while dark/chromatic closeup foreground falls about 16 percent, detected wood share rises 6.9 percentage points, and exact 60 m source foreground falls about 10 percent. `futaleufu_cordillera_cypress_v20_3_hierarchical_shoot_visual_review.json` hash-locks the clean first-run captures, retains the shader gate, rejects the exact V20.3 geometry and every promotion path, and leaves V20.2 as the source baseline. The next pass should preserve V20.2 parent spray mass and test shorter low-wood terminal clusters near branchlet ends, with a positive 60 m density gain required before accepting more geometry or beginning HLOD handoff calibration.

Cordilleran-cypress V20.4 tests that bounded low-wood terminal-cluster alternative and rejects it on cost. The complete V20.2 parent topology returns, while each lateral gains two short terminal clusters near its tip and only 28 percent of each terminal length receives woody support. The new material namespace renders valid foliage and alpha on its first run. Exact comparison against V20.2 records 6.5 percent more closeup foreground, about one percentage point less detected wood, 1.9 percent more source foreground at 60 m, and a bounded 1.135 HLOD/source foreground ratio. Those small gains require 3.35 million merged raster triangles, an 87.7 percent increase over V20.2's 1.79 million. `futaleufu_cordillera_cypress_v20_4_terminal_cluster_visual_review.json` therefore rejects the geometry and every promotion path, keeps V20.2 as the source baseline, and closes further per-twig terminal-card growth. The next source decision must use a lower-cost crown-level representation or a separately rights-reviewed distance-stable native asset before exact 20/28/36 m authority, temporal, desktop, or target-VR gates resume.

Cordilleran-cypress V21 clears that lower-cost source gate without claiming finished tree art. A deterministic first-party generator composes six independently generated V20 measured sprays into each of sixteen 36-44 cm compound branchlet atlas tiles, clears the V20 mip-padding RGB before geometric resampling, reapplies tile-safe padding only after composition, and records every placement, hash, alpha statistic, and provenance boundary. Unreal keeps V20.2's six connected lateral pairs, seed, 3,626 foliage transforms, open-grown form, cameras, and source-lit HLOD path, but reduces each authored twig from 27 curved cards to 14 and uses an entirely new material namespace. The clean first run succeeds. Against V20.2, merged source triangles fall 21.1 percent from 1.79 million to 1.41 million while dark/chromatic closeup foreground rises 27.6 percent, detected wood share falls 7.2 percentage points, and exact 60 m source foreground rises 50.5 percent. The HLOD/source foreground ratio improves to 1.057 and atlas coverage reaches 0.128-0.143. `futaleufu_cordillera_cypress_v21_compound_branchlet_visual_review.json` therefore retains V21 as the new open-grown source baseline, but rejects all-form, corridor, and production promotion: the whole crown still reads as coarse horizontal tiers with large open bands, and close inspection still exposes a regular procedural woody fan and flattened compound source plane. The next isolated pass must break crown-level tier rhythm under the same cost and exact-camera guardrails before 20/28/36 m authority, temporal, desktop, target-VR, or mixed-ecology work resumes.

Cordilleran-cypress V22 makes the first measured whole-crown topology improvement while keeping those promotion gates closed. A separate PVE mode and material/asset namespace reuse V21's compound branchlet textures and fourteen-card authored twig, but replace coarse main-axis priority steps with a gradual six-key ramp, apply 137.507764-degree golden-angle main and graft phyllotaxy, increase bounded stagger and angle jitter, reduce branch flattening, and stop graft distribution at 0.98 normalized height. A reproducible metric examines the upper 82 percent of the centrally detected crown in exact 35- and 145-degree turntable views: after 11-pixel row smoothing and 90th-percentile normalization, mean rows below 0.25 occupancy fall from 10.06 to 7.59 percent and the largest open run falls from 51 to 46 pixels. The resulting tree uses 3,303 foliage instances and 1.29 million source triangles, reductions of 8.9 and 8.7 percent from V21; closeup dark/chromatic foreground changes only -0.8 percent, exact 60 m source foreground falls a bounded 13.0 percent, and HLOD/source foreground remains 1.068. `futaleufu_cordillera_cypress_v22_detiered_crown_visual_review.json` therefore retains the golden-angle scheduling as the open-grown crown-topology baseline, but rejects visual, all-form, corridor, and production promotion because the crown remains too sparse and broad shelf rhythms remain legible. The next isolated pass must add asynchronous secondary shoot clusters that restore irregular fine-scale mass without returning to synchronized tiers or indiscriminately multiplying per-twig cards.

Cordilleran-cypress V23 adds that asynchronous variation without adding per-twig cards. The main grower, graft density, golden-angle attachment schedule, V21 fourteen-card compound branchlet, cameras, and source-lit HLOD path remain fixed. The graft palette instead contains three six-cycle branch growers with seed offsets 7,919, 112,648, and 217,377; distinct bounded secondary-shoot priority ramps; 4, 71, and 223.5-degree branchlet phyllotaxy; and 0.04, 0.07, and 0.10 stagger. Unreal's existing unconditioned distribution picker selects one candidate uniformly and deterministically at each existing graft attachment. In the exact 35- and 145-degree views, mean low-occupancy crown rows fall from 7.59 to 7.05 percent and the largest open run falls from 46 to 45 pixels. Closeup foreground changes only +0.12 percent, exact 60 m source foreground changes -2.27 percent, and HLOD/source foreground remains 1.069, while source triangles and foliage instances both fall about 9.4 percent to 1.17 million and 2,993. `futaleufu_cordillera_cypress_v23_async_secondary_crown_visual_review.json` therefore retains V23 as the new open-grown topology baseline. It still rejects visual, all-form, corridor, and production promotion: the primary scaffold reads as a sparse ladder of broad shelves, and the HLOD remains darker and more saturated than the source. The next isolated crown pass must replace that remaining primary-shelf rhythm with irregular crown-level mass under the same cost and exact-camera guardrails; HLOD photometry, exact 20/28/36 m authority, temporal behavior, and packaged desktop/target-VR evidence follow before all-form regeneration.

Cordilleran-cypress V24 retains the V21 compound branchlet source, V22 de-tiered golden-angle main crown, and V23 three-template asynchronous graft palette while replacing Unreal's default linear 1.0-to-0.1 graft-scale taper with a smooth six-key 0.92/0.96/0.98/0.94/0.82/0.48 plant-height envelope and deterministic 0.88-to-1.12 attachment variation. The first unbalanced run is explicitly rejected: it raised foliage instances from 2,993 to 5,241 and merged source triangles from 1.17 million to 1.97 million. Increasing live-foliage spacing from 0.055 to 0.096 compensates the measured foliage-bearing path growth. The retained run uses 2,944 instances and 1.214 million source triangles, leaves closeup foreground effectively unchanged, adds 17.20 percent exact 60 m source mass, and keeps HLOD/source foreground at 1.098. Across the exact 35- and 145-degree turntables, mean low-occupancy rows fall from 7.05 to 5.60 percent and the largest open run falls from 45 to 24 pixels. `futaleufu_cordillera_cypress_v24_irregular_crown_mass_visual_review.json` retains V24 as the bounded open-grown topology baseline, but rejects visual, all-form, corridor, and production promotion: the regular central scaffold and flattened closeup source planes remain visible, while the HLOD is still darker and more saturated than the exact source. The next gate must calibrate source/HLOD photometry and prove exact 20/28/36 m authority before closeup source/scaffold correction, temporal and platform evidence, or family regeneration.

Cordilleran-cypress V25 separates HLOD calibration and representation authority from the still-open no-pop gate. It keeps V24's 2,944 foliage instances, 1.214 million merged source triangles, crown topology, source material, atlas bake, and cameras unchanged while raising the source-lit atlas linear gain from 1.10/1.38/0.90 to 2.60/1.63/1.60. Across 47,573 intersecting exact-60 m foreground pixels, HLOD minus source mean RGB is now -0.80/-0.14/-3.97 levels and HLOD/source foreground remains bounded at 1.090. Nine new source-only, HLOD-only, and combined captures prove hard one-representation authority at a 25 m horizontal actor-distance threshold: combined is byte-identical to HLOD at 28 and 36 m and differs from the temporally lit source at 20 m by only 0.0068 percent of pixels with a 0.0033-level mean delta. `futaleufu_cordillera_cypress_v25_hlod_handoff_visual_review.json` retains those mechanisms but rejects the actual handoff. Source/HLOD changed-pixel fractions remain 42.02, 29.48, and 21.66 percent at 20/28/36 m, foreground IoU is only 0.499-0.538, and the HLOD is visibly blurrier and fuller. The next pass must improve atlas spatial registration and branch-scale detail or prove a bounded complementary dither transition before moving-camera, platform, family, corridor, or production review.

Cordilleran-cypress V26 tests whether atlas resolution is the missing branch-scale-detail variable and uncovers a more fundamental capture defect. The baker now accepts explicit 512- or 1024-pixel-per-view profiles, and the V26 run raises the sixteen-view atlas from 2048 to 4096 pixels while preserving V24 geometry, V25 color gain, exact cameras, and hard 25 m authority. That quadruples texel area without improving the no-pop evidence: source/HLOD changed-pixel fractions are 42.49, 29.95, and 22.13 percent at 20/28/36 m, with foreground IoU of 0.504-0.538. More importantly, V25-to-V26 source-only frames change 1.63-2.87 percent even though their source geometry is identical, and their HLOD frames change 14.87-27.41 percent. Inspection traces that invalid isolation to the live source material's nonzero `SimpleGrassWind` defaults, which allow separate runs and sequential atlas views to capture different poses. `futaleufu_cordillera_cypress_v26_high_detail_hlod_visual_review.json` therefore retains only the flexible baker and rejects the 1024 profile, runtime adoption, and any conclusion about resolution. The next experiment must freeze wind/WPO for both source controls and every atlas view, then rerun matched 512 and 1024 profiles before choosing registration, detail, or complementary-dither work.

Cordilleran-cypress V27 establishes the missing 512-pixel deterministic validation control without changing production wind or claiming visual quality. A separate material and generated-review namespace set `WindIntensity`, `WindWeight`, and `WindSpeed` to zero; V24 geometry, V25 color gain, hard authority, 2,944 foliage instances, and 1.214 million source triangles remain fixed. Two independent executions produce byte-identical foliage-template JSON, proving all generated transforms are stable. Frozen WPO alone still leaves render variation, so V27 explicitly uses an unlit, no-post-process validation profile with AA, temporal effects, GI, AO, atmosphere, fog, and reflections disabled. This is not an art-review image. Under that bounded Metal raster profile, repeated source-only frames change 1.04, 0.76, and 0.59 percent at 20/28/36 m; HLOD-only frames change 1.15, 0.95, and 0.75 percent; all four atlas channels stay below 1.76 percent; and hard one-representation authority passes. `futaleufu_cordillera_cypress_v27_frozen_wpo_repeatability_review.json` retains V27 as the 512 validation baseline. The next experiment must generate a 1024 profile under the identical contract, compare against these measured noise floors, and only then return any winning detail decision to lit photoreal review.

Cordilleran-cypress V28 completes that matched comparison and rejects resolution as the current handoff fix. It holds V27 geometry, transform template, zero-wind material, deterministic unlit renderer, V25 gain and authority, cameras, and output channels fixed while changing only the tile from 512 to 1024 pixels and the atlas from 2048 to 4096. V27-to-V28 source-only changes remain within the measured raster floor at 1.06, 0.80, and 0.58 percent, so the experiment is isolated; HLOD frames change 19.08, 13.84, and 10.23 percent. That material change moves away from the source: source/HLOD changed-pixel fractions increase from 42.52/30.18/22.18 to 42.87/30.43/22.38 percent at 20/28/36 m, while foreground IoU falls by 0.011/0.009/0.008. `futaleufu_cordillera_cypress_v28_frozen_wpo_resolution_review.json` retains the V27 512 validation baseline and flexible baker but rejects the V28 runtime profile, a resolution-only fix, and the four-times texel-area cost. The next isolated work must address atlas spatial registration or representation shape, then evaluate a bounded complementary dither transition before returning to lit photoreal review.

Cordilleran-cypress V29 resolves the first registration question and retains a narrow improvement. The authored handoff cameras approach the tree at 192.53 to 201.80 degrees, but V27's zero-phase 45-degree grid selects the 180-degree frame, leaving 12.53 to 21.80 degrees of view error. V29 keeps V27 source geometry, zero wind, 512-pixel tiles, deterministic unlit renderer, orthographic projection, photometry, output channels, and authority fixed while shifting both capture directions and runtime frame selection by 16 degrees. The nearest-frame errors fall to 0.05 to 5.80 degrees; source controls remain within the measured raster floor at 1.12/0.83/0.65 percent; changed-pixel fraction improves by 0.98/0.77/0.64 percentage points; foreground IoU improves by 0.009/0.005/0.002; and hard authority passes. `futaleufu_cordillera_cypress_v29_azimuth_registration_review.json` retains the registration mechanism, but V29 still fails the no-pop gate at 41.54/29.41/21.54 percent changed pixels and 0.697/0.780/0.836 IoU. These validation-only captures are not art evidence. Keep the registered 512 diagnostic and next isolate perspective projection or a depth-aware representation shape before evaluating complementary dither and returning to lit photoreal review.
