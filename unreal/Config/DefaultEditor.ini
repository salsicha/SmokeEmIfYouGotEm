; Editor viewports (including PIE) default to resolution-scaled screen
; percentage, so the water renders through an upscaler at reduced input
; resolution in-editor while -game runs native. That gap made reflections
; and specular detail visibly noisier/flickerier in PIE than in the shipped
; presentation (water flicker investigation, 2026-08-27). Pin realtime
; editor viewports to true 100 % so in-editor review matches -game.
[/Script/UnrealEd.EditorPerformanceProjectSettings]
RealtimeScreenPercentageMode=Manual
ManualScreenPercentage=100.000000
